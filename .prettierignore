dist
node_modules
preview/dist
coverage
.turbo

# Generated artifacts are owned by their generator (single source of truth);
# their format is enforced by the generator's own --check, not prettier.
# e.g. mcp/src/data/component-tokens.generated.ts (scripts/gen-component-tokens.mjs)
*.generated.ts

# Package-manager lockfiles are tool-owned (pnpm), not hand-formatted.
pnpm-lock.yaml

# Frame-gate artifacts, owned by scripts/check-frame-axe.mjs / frame-geometry.mjs /
# frame-coverage.mjs — regenerated deterministically, so prettier must not fight the generator.
scripts/frame-axe.baseline.json
scripts/frame-geometry.baseline.json
docs/FRAME-COVERAGE-REPORT.md

# Issue #163 contract ledger, owned by scripts/gen-frame-coverage-ledger.mjs (its --check is the
# formatter of record). Prettier must not reflow it, or `check:frame-coverage-ledger` and
# `pnpm format` would each demand the other's output.
preview/frame-coverage.ledger.json
