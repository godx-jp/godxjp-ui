# Query

Async lifecycle primitives for TanStack Query and React Router composition.

Included primitive pages: DataState, MutationFeedback, QueryRefetchButton, InfiniteQueryState and PrefetchLink.

Query primitives are behavioral wrappers: they standardize loading, empty, error, retry, load-more and prefetch patterns while visual children stay composed from primitive UI.
