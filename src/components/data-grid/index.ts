// @godxjp/ui/data-grid — TanStack Table adapter subpath. Pulls @tanstack/react-table (a peer),
// so it is intentionally NOT re-exported from the runtime-neutral root barrel (src/index.ts).
export { DataGrid } from "./data-grid";
export type { DataGridProps, DataGridDensity, ColumnDef } from "./data-grid";
