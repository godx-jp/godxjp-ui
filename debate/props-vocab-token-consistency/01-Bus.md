# 01 — Message Bus (append-only)

---

### r0 · ADV-SQ → all · opening

claim: STATUS-QUO+ is the right standard: preserve the current component-aware vocabulary and token layers, while fixing only unarguable defects: remove `--tracking-*`, collapse the gap prop family into one axis-aware `GapProp`, and repair registry entries that reference or omit existing prop concepts.
evidence: `GapProp` is already a deprecated alias to `StackGapProp` while `InlineGapProp`/`StackGapProp` are axis variants of the same `gap` prop (`src/props/vocabulary/layout.prop.ts:12`, `src/props/vocabulary/layout.prop.ts:15`, `src/props/vocabulary/layout.prop.ts:24`); domain tokens are defined in both light and dark themes and exported through Tailwind theme colors (`src/tokens/foundation.css:33`, `src/tokens/foundation.css:148`, `src/styles/index.css:56`); the registry describes `PageTitleProp` as an alias of `TitleProp` rather than a distinct concept (`src/props/registry.ts:233`, `src/props/registry.ts:236`); the registry references `UseZodFormReturnProp` and `ZodSchemaProp` although the vocabulary registry has no entries for them (`src/props/registry.ts:622`, `src/props/registry.ts:625`, `src/props/components/form.prop.ts:8`, `src/props/components/form.prop.ts:17`); MUI, Chakra, Ant Design, and Radix all keep component-scoped variant/state props where semantics differ, while Radix controlled components commonly use `value/defaultValue/onValueChange` and `open/defaultOpen/onOpenChange`; W3C DTCG defines token objects/groups with `$value`/`$type`, not a requirement that every public token be semantic-only.
warrant: The repo already distinguishes real concepts in comments and component APIs; broad renames would spend migration cost on names that peer frameworks also keep component-scoped. The defects above are mechanically provable, domain-neutrality/enforceability violations, so fixing them improves consistency without pretending every repeated word is the same concept.
assumptions: The standard is for the current public TypeScript/CSS API, not a greenfield redesign; breaking renames need higher proof than alias cleanup; deprecated component props can remain documented as compatibility shims until a planned major.
confidence: 82

### r0 · ADV-STRICT → all · opening

claim: STRICT-CANON is the right standard: collapse every duplicate prop concept into one canonical vocabulary, require controlled/open vocab and generic Size/Variant/Tone/Density/Gap/Title props in the registry, and publish only W3C-DTCG-style semantic/component tokens after deleting domain tokens.
evidence: Registry duplicates: StackGapProp/InlineGapProp plus deprecated GapProp (`src/props/registry.ts:164`, `src/props/registry.ts:169`, `src/props/vocabulary/layout.prop.ts:24`); PageTitleProp is explicitly an alias of TitleProp (`src/props/registry.ts:233`); component registry references absent vocabulary names UseZodFormReturnProp/ZodSchemaProp (`src/props/registry.ts:622`, `src/props/registry.ts:625`, absent before `src/props/registry.ts:290`); controlled props exist locally but not canonically (`src/props/components/data-entry.prop.ts:71`, `src/props/components/data-entry.prop.ts:85`, `src/props/components/navigation.prop.ts:81`, `src/props/components/navigation.prop.ts:82`); domain tokens are defined and publicly exported (`src/tokens/foundation.css:33`, `src/tokens/foundation.css:35`, `src/styles/index.css:56`, `src/styles/index.css:58`). Radix Themes uses stable public concepts such as size/variant/color/asChild and a 9-step spacing scale; MUI System maps system props to theme tokens; W3C DTCG standardizes token exchange and aliases; Style Dictionary documents DTCG compatibility and CTI/path-based token organization.
warrant: The repo already has a registry, but the registry permits aliases, missing entries, empty mappings, and component-prefixed forks; only a strict canonical registry plus audit can make prop APIs predictable. The token files already mix primitive palettes, semantic roles, component tokens, domain names, and public Tailwind exports; only primitive→semantic→component layering with semantic-only public exports prevents app/domain leakage and raw-token drift.
assumptions: Public exported component prop types are API surface; CSS variables and Tailwind `@theme` values are public token surface; migration cost is secondary under the topic rubric and hard constraints; external framework facts are used as standards evidence, not as claims that their exact enum values must be copied.
confidence: 91

### r0 · ADV-PRAGMATIC → all · opening

claim: PRAGMATIC-SHARED is the right standard: make cross-cutting prop concepts and token layers canonical, but keep component-specific enums when their values describe different semantics rather than forcing one global VariantProp.
evidence: Repo: shared vocab has value/onChange but no defaultValue/onValueChange (src/props/vocabulary/shared.prop.ts:49-53), while components already use defaultValue/onValueChange/onChange for abstract values (src/props/components/data-entry.prop.ts:69-86, src/props/components/data-entry.prop.ts:214-218); Stack/Inline split gap types for the same public gap prop (src/props/vocabulary/layout.prop.ts:12-16, src/props/components/layout.prop.ts:33-41); Badge inlines semantic status values as variant despite StatusToneProp existing (src/props/vocabulary/interaction.prop.ts:29-33, src/props/components/data-display.prop.ts:43-49); foundation leaks --tracking-internal/--tracking-seller/--tracking-yamato and exposes them publicly (src/tokens/foundation.css:33-35, src/tokens/foundation.css:148-150, src/styles/index.css:56-58). Peer facts: MUI Button keeps button-specific variant text/contained/outlined while Alert separates severity from variant; Ant Button uses button-specific type primary/default/dashed/text/link while Alert has status type plus style variant; W3C DTCG treats token $type as value-shape metadata and groups as arbitrary categories, so layered naming and aliases are compatible with standards.
warrant: The repo has real duplicate cross-cutting concepts that should be canonicalized, but ButtonVariant, PageContainerVariant, UploadVariant, and Alert style/status semantics are not one interchangeable enum; peer frameworks also share size/color-ish concepts while retaining per-component unions. Token cleanup should remove domain leakage and clarify primitive/semantic/component layers instead of flattening every token into one public semantic surface.
assumptions: The library is intended as a reusable domain-neutral UI package; current files outside src/props, src/tokens, and src/styles were not inspected under blind rules; breaking renames are acceptable when they buy enforceable API clarity.
confidence: 88

### r1 · SKEPTIC → all · rebuttal

claim: NONE. STRICT-CANON false-merges real concepts, STATUS-QUO+ leaves enforceability holes open, and PRAGMATIC-SHARED still overcommits to a token taxonomy and renames before proving consumer value. The standard should be narrower: register every actual public prop, add missing controlled-state vocabulary when already used, ban domain tokens, collapse only exact aliases/same-prop same-role duplicates, and require an explicit local-vs-shared decision for every enum/token instead of preselecting STRICT, PRAGMATIC, or SQ.
steelman: STRICT-CANON's strongest case is auditability: one vocabulary name per public concept, controlled/open triples, and primitive->semantic->component token tiers make the registry enforceable instead of advisory. STATUS-QUO+'s strongest case is migration discipline: `ButtonVariantProp`, `AlertVariantProp`, `PageContainerVariantProp`, and density props are not automatically the same concept merely because names overlap, so broad renames can reduce clarity. PRAGMATIC-SHARED's strongest case is the real middle: shared `gap`, controlled value/open vocabulary, and domain-token removal are provable defects, while component-specific enums can stay when the registry documents distinct meaning.
evidence: `ButtonVariantProp` is button visual treatment (`default|destructive|outline|secondary|ghost|link`) while `AlertVariantProp` is alert status tone (`default|destructive|warning|success`) and `ConfirmVariantProp` is confirm emphasis (`default|destructive`) (`src/props/vocabulary/interaction.prop.ts:6`, `src/props/vocabulary/interaction.prop.ts:26`, `src/props/vocabulary/interaction.prop.ts:32`); `PageContainerVariantProp` is page-shell layout, not visual treatment (`src/props/vocabulary/layout.prop.ts:9`); `UploadVariantProp` is upload UI mode (`dropzone|button|picture-card|picture|avatar|avatar-crop`) (`src/props/components/data-entry.prop.ts:293`). That makes one global `VariantProp` a false merge. The repo does have exact/shared defects: `StackGapProp` and `InlineGapProp` are the same public `gap` prop with axis-specific subsets (`src/props/vocabulary/layout.prop.ts:12`, `src/props/vocabulary/layout.prop.ts:15`), `GapProp` is already only a deprecated alias (`src/props/vocabulary/layout.prop.ts:24`), `PageTitleProp` is registered as an alias of `TitleProp` (`src/props/registry.ts:233`), and `DefaultValueProp`/`OnValueChangeProp` are absent even though components expose `defaultValue` and `onValueChange` (`src/props/vocabulary/shared.prop.ts:49`, `src/props/vocabulary/shared.prop.ts:52`, `src/props/components/data-entry.prop.ts:70`, `src/props/components/data-entry.prop.ts:85`, `src/props/components/data-entry.prop.ts:216`). STATUS-QUO+ underestimates registry breakage: `FormRootProp` references `UseZodFormReturnProp`/`ZodSchemaProp` with no vocabulary entries (`src/props/registry.ts:622`), and navigation items have empty vocabulary arrays despite public fields (`src/props/registry.ts:614`, `src/props/registry.ts:621`). Token evidence cuts both ways: `--tracking-*` is a clear domain leak in foundation and Tailwind public exports (`src/tokens/foundation.css:33`, `src/tokens/foundation.css:148`, `src/styles/index.css:56`), but Tailwind v4's `@theme` is itself the public design-token surface (`src/styles/index.css:22`) and the package already exports semantic roles plus approved decorative wa-iro utilities (`src/styles/index.css:29`, `src/styles/index.css:65`). The over-engineered tiers are STRICT's private primitive palette rule, mandatory W3C-style semantic-only public surface, full component-token layer for every raw value, and density micro-tokenization for checkbox/switch dimensions (`src/styles/density.css:12`, `src/styles/density.css:36`); W3C DTCG standardizes `$value`, `$type`, aliases, and groups as an exchange format, not a requirement that Tailwind libraries hide every primitive token (https://www.w3.org/community/reports/design-tokens/CG-FINAL-format-20251028/). Peer frameworks argue against global enum collapse: MUI Button `variant` is text/contained/outlined while Alert separates `severity` from style `variant` (https://mui.com/material-ui/react-button/, https://mui.com/material-ui/api/alert/); Ant Design Button `type` is action treatment while Alert has status `type` and style `variant` (https://ant.design/components/button/, https://ant.design/components/alert/); Radix Themes shares prop names like `size`/`variant`/`color` but still documents component props per component rather than one global TypeScript enum (https://www.radix-ui.com/themes/docs/components/button).
warrant: The decisive test is whether the same prop spelling carries the same value kind and semantic role. `gap` passes; `TitleProp`/`PageTitleProp` passes as an alias; controlled `value/defaultValue/onValueChange` passes as missing shared vocabulary. `VariantProp` fails because the values are not interchangeable and the comments define different domains; `DensityProp` is risky because table rows omit `default` while page density includes it (`src/props/vocabulary/layout.prop.ts:6`, `src/props/vocabulary/layout.prop.ts:18`). A full controlled-vocab rename is not worth the breakage where components already expose public `onChange` payload callbacks; add `DefaultValueProp` and `OnValueChangeProp`, reserve new APIs for `onValueChange`, but do not force an immediate rename of every abstract `onChange`. Biggest risk per option: STRICT-CANON breaks semantic precision by collapsing distinct enums into one misleading type; STATUS-QUO+ normalizes an incomplete registry and lets future ad-hoc props keep slipping through; PRAGMATIC-SHARED becomes STRICT-by-policy creep, especially in token tiers, without a clear test for when a primitive/decorative Tailwind token is acceptable.
confidence: 80 (delta: +7, because: ADV-PRAGMATIC's specific argument that Button/PageContainer/Upload variants are not one concept proves STRICT's largest false-merge risk; ADV-STRICT's specific registry-empty/missing-controlled-vocab argument proves STATUS-QUO+ is under-scoped; ADV-SQ's specific W3C-DTCG argument that groups/tokens do not require semantic-only public output proves STRICT's token tier is over-engineered)

### r1 · ADV-SQ → all · rebuttal

claim: STATUS-QUO+ remains the governing standard: keep the existing component-aware vocabulary and CSS-token model, but make the clear defects mandatory cleanup: delete domain `--tracking-*` tokens/exports, collapse `StackGapProp` + `InlineGapProp` + deprecated `GapProp` into one canonical `GapProp` concept, and make the registry truthful for existing public props. Do not mandate broad public renames to generic `VariantProp`, `DensityProp`, `TitleProp`, or semantic-only public tokens.
steelman: STRICT's strongest case is that a professional design system needs an auditable contract, and the current registry is not that contract while it permits missing vocabulary names, empty component mappings, duplicate aliases, and mixed token layers. STRICT is also right that controlled state has an industry-recognized triple (`value/defaultValue/onValueChange`, `open/defaultOpen/onOpenChange`) and that package CSS variables/Tailwind exports are public API. PRAGMATIC's strongest case is narrower and stronger: shared vocabulary must be mandatory for cross-cutting concepts, but component-specific enums should survive when their values mean different things; it also correctly says tokens should be layered and domain-neutral with documented app-layer escape hatches.
evidence: The two conceded defects are direct: `StackGapProp` and `InlineGapProp` both type the public `gap` prop, while deprecated `GapProp` aliases only stack (`src/props/vocabulary/layout.prop.ts:12`, `src/props/vocabulary/layout.prop.ts:15`, `src/props/vocabulary/layout.prop.ts:24`, `src/props/components/layout.prop.ts:35`, `src/props/components/layout.prop.ts:40`); `--tracking-internal`, `--tracking-seller`, and `--tracking-yamato` are defined in light/dark foundation tokens and exported through Tailwind theme (`src/tokens/foundation.css:33`, `src/tokens/foundation.css:34`, `src/tokens/foundation.css:35`, `src/tokens/foundation.css:148`, `src/tokens/foundation.css:149`, `src/tokens/foundation.css:150`, `src/styles/index.css:56`, `src/styles/index.css:57`, `src/styles/index.css:58`). Registry truth also needs repair because `FormRootProp` references absent vocabulary names (`src/props/registry.ts:622`, `src/props/registry.ts:625`) and `defaultValue`/`onValueChange` are used by real components while shared vocabulary only has DOM-oriented `ValueProp`/`OnChangeProp` (`src/props/components/data-entry.prop.ts:71`, `src/props/components/data-entry.prop.ts:85`, `src/props/components/data-entry.prop.ts:217`, `src/props/vocabulary/shared.prop.ts:49`, `src/props/vocabulary/shared.prop.ts:52`). But STRICT overreaches on global concept collapse: the repo documents `PageDensityProp` as page/control density and `TableDensityProp` as row density "NOT the same" (`src/props/vocabulary/layout.prop.ts:6`, `src/props/vocabulary/layout.prop.ts:18`, `src/props/registry.ts:174`), and variants are explicitly different concepts: button visual style, confirm emphasis, alert banner tone, page shell layout, and upload presentation (`src/props/vocabulary/interaction.prop.ts:6`, `src/props/vocabulary/interaction.prop.ts:26`, `src/props/vocabulary/interaction.prop.ts:32`, `src/props/vocabulary/layout.prop.ts:9`, `src/props/components/data-entry.prop.ts:294`). Peer-framework evidence supports this split: MUI keeps Button `variant` separate from Alert `severity`/`variant`; Ant keeps Button `type` separate from Alert status/style; Radix controlled components commonly use `value/defaultValue/onValueChange` and `open/defaultOpen/onOpenChange` without proving that every component enum is one global enum. W3C DTCG requires token objects with `$value`/`$type` and supports references/groups; it does not require a CSS library to hide every primitive palette token from the public surface.
warrant: A standard is enforceable when it forbids mechanically provable drift: domain tokens, duplicate `gap` aliases, registry references to absent vocabulary, and unregistered reused controlled-state concepts. It becomes counterproductive when it treats repeated words as proof of identical concepts. The current code already marks several differences as semantic differences, and peer frameworks do the same. STATUS-QUO+ therefore absorbs PRAGMATIC's strongest cross-cutting fixes without accepting STRICT's broad rename program or PRAGMATIC's layer reorganization as mandatory breaking work.
confidence: 79 (delta: -3, because: PRAGMATIC's specific argument that `defaultValue`/`onValueChange` are already reused cross-cutting concepts proves STATUS-QUO+ must include registry/vocabulary repair for those concepts, not only the two headline defects)

### r1 · ADV-PRAGMATIC → all · rebuttal

claim: PRAGMATIC-SHARED still wins: canonize shared state/layout/content concepts and token layers, but permit component-specific enums where the value set encodes different semantics. Concessions: `gap` is one public concept and should be one `GapProp`; controlled abstract values need `value/defaultValue/onValueChange`; disclosure needs `open/defaultOpen/onOpenChange`; `PageTitleProp` is truly the same content concept as `TitleProp` and should become a deprecated compatibility alias.
steelman: STRICT's strongest case is enforceability: the registry already tolerates missing entries, alias forks, empty mappings, local `defaultValue`/`onValueChange`, duplicate `gap`, duplicate `title`, and public domain tokens, so only a hard canonical vocabulary plus audit can prevent drift. STRICT is also right that semantic tone values should not hide behind `variant`, especially Badge/Alert status values. SQ's strongest case is migration discipline: component variants and densities are not automatically the same concept, peers keep component-scoped props, W3C-DTCG does not require semantic-only public tokens, and breaking public aliases without proof wastes the migration budget.
evidence: `ValueProp` and DOM-event `OnChangeProp` exist, but no `DefaultValueProp`/`OnValueChangeProp` exists in shared vocabulary (`src/props/vocabulary/shared.prop.ts:49`, `src/props/vocabulary/shared.prop.ts:53`), while components already expose `defaultValue`/`onValueChange` (`src/props/components/data-entry.prop.ts:71`, `src/props/components/data-entry.prop.ts:85`, `src/props/components/navigation.prop.ts:81`, `src/props/components/navigation.prop.ts:82`). `OpenProp`/`OnOpenChangeProp` exist without `DefaultOpenProp` (`src/props/vocabulary/shared.prop.ts:16`, `src/props/vocabulary/shared.prop.ts:20`). `StackGapProp` and `InlineGapProp` are both the prop spelling `gap`, and `GapProp` is already a deprecated alias (`src/props/vocabulary/layout.prop.ts:12`, `src/props/vocabulary/layout.prop.ts:16`, `src/props/vocabulary/layout.prop.ts:24`, `src/props/components/layout.prop.ts:35`, `src/props/components/layout.prop.ts:40`). `PageTitleProp` is explicitly "Same as TitleProp" and `PageContainerProp.title` consumes it (`src/props/vocabulary/navigation.prop.ts:18`, `src/props/vocabulary/navigation.prop.ts:19`, `src/props/components/layout.prop.ts:20`). But `ButtonVariantProp` is action visual treatment (`src/props/vocabulary/interaction.prop.ts:7`), `PageContainerVariantProp` is page shell layout (`src/props/vocabulary/layout.prop.ts:9`, `src/props/components/layout.prop.ts:26`), `UploadVariantProp` is upload UI mode (`src/props/components/data-entry.prop.ts:294`), and Tabs inline `variant` is tab presentation (`src/props/components/navigation.prop.ts:83`); those are not one interchangeable enum. Badge proves the tone cleanup point because one `variant` union mixes visual treatments and statuses (`src/props/components/data-display.prop.ts:45`) while `StatusToneProp` already names status color intent (`src/props/vocabulary/interaction.prop.ts:29`). Domain tokens are public defects (`src/tokens/foundation.css:33`, `src/tokens/foundation.css:148`, `src/styles/index.css:56`), while decorative/primitive wa colors are explicitly documented as chart/tag/decoration accents (`src/tokens/foundation.css:37`, `src/styles/index.css:65`). Peer-framework evidence: MUI keeps Button `variant` separate from Alert `severity`/`variant`; Ant keeps Button `type` separate from Alert `type`/`variant`; Radix controlled components commonly use `value/defaultValue/onValueChange` and `open/defaultOpen/onOpenChange`. W3C-DTCG evidence: the format defines token objects via `$value`/`$type`, token references/aliases, and arbitrary groups; it standardizes exchange shape, not a rule that every consumed CSS variable must be semantic-only.
warrant: The governing rule should be concept identity, not word identity. When the public prop spelling, value kind, and semantic role are the same (`gap`, `title`, controlled value/open), one canonical vocabulary name improves prediction and auditability. When the same word carries different component-local semantics (`variant` for button treatment vs page layout vs upload mode), a global enum reduces clarity and breaks peer-framework alignment. Token standardization likewise requires domain-neutral layered ownership and audit checks, but SQ under-fixes registry gaps and STRICT over-fixes by treating all primitive/decorative public exports as defects instead of documented low-level tokens.
confidence: 90 (delta: +2, because: STRICT's evidence that `variant` currently carries tone values strengthens the need for shared `ToneProp`, but SQ's peer-framework argument against one global `VariantProp` is correct and narrows the canonicalization boundary.)

### r1 · ADV-STRICT → all · rebuttal

claim: STRICT-CANON remains the governing standard: every reused public prop concept must have one canonical vocabulary entry, every component prop must map to the registry, controlled state must use `value/defaultValue/onValueChange` and `open/defaultOpen/onOpenChange`, and tokens must be split primitive -> semantic -> component with no domain tokens in the package public surface. Concession: a per-component enum may remain only when it is genuinely a distinct concept, such as page shell layout or upload mode, and the registry records why it is local rather than shared.
steelman: PRAGMATIC's strongest case is that not every repeated word is the same concept: `ButtonVariantProp` describes action treatment, `PageContainerVariantProp` describes page shell layout, and upload `variant` describes upload UI mode (`src/props/vocabulary/interaction.prop.ts:6-13`, `src/props/vocabulary/layout.prop.ts:9-10`, `src/props/components/data-entry.prop.ts:293-304`); peer frameworks also keep per-component style enums, e.g. MUI Button `variant` differs from MUI Alert `severity`/`variant`, and Ant Button `type` differs from Alert status/style. SQ's strongest case is migration discipline: fix mechanically provable defects first, especially `--tracking-*`, gap aliases, absent `DefaultValueProp`/`OnValueChangeProp`, missing form vocabulary entries, and empty registry arrays (`src/tokens/foundation.css:33-35`, `src/styles/index.css:56-58`, `src/props/vocabulary/layout.prop.ts:12-24`, `src/props/components/data-entry.prop.ts:69-86`, `src/props/registry.ts:622-625`, `src/props/registry.ts:320-326`).
evidence: The current registry already proves canonical enforcement is failing: `StackGapProp`/`InlineGapProp` split one public `gap` concept while `GapProp` is deprecated (`src/props/registry.ts:164`, `src/props/registry.ts:169`, `src/props/vocabulary/layout.prop.ts:24`, `src/props/components/layout.prop.ts:35-40`); `PageTitleProp` is explicitly an alias of `TitleProp` (`src/props/registry.ts:233-236`); `PageDensityProp`/`TableDensityProp` fork density while density CSS retunes cross-component control/table values from one density layer (`src/props/vocabulary/layout.prop.ts:6-21`, `src/styles/density.css:7-41`); abstract value changes use both DOM `OnChangeProp` and local `onValueChange` without a shared vocabulary (`src/props/vocabulary/shared.prop.ts:49-53`, `src/props/components/data-entry.prop.ts:71-86`, `src/props/components/navigation.prop.ts:81-82`); status color intent is named as both tone and variant (`src/props/vocabulary/interaction.prop.ts:29-33`, `src/props/components/data-display.prop.ts:43-49`); form registry references vocabulary names absent from `VOCABULARY_REGISTRY` (`src/props/registry.ts:622-625`, `src/props/registry.ts:290`); domain tokens are defined and publicly exported (`src/tokens/foundation.css:33-35`, `src/tokens/foundation.css:148-150`, `src/styles/index.css:56-58`). Radix Themes exposes stable shared concepts such as `size`, `variant`, `color`, `asChild`, and common spacing props; MUI System maps system props through theme tokens; W3C DTCG defines token `$value`, `$type`, aliases/references, and groups for exchange, which supports explicit primitive/semantic/component layering rather than domain nouns mixed into public CSS.
warrant: PRAGMATIC correctly identifies the exception, but the exception strengthens STRICT rather than replacing it: a component-local enum is valid only after proving it is not a shared concept, while today the registry allows aliases, missing concepts, empty mappings, and overloaded names. SQ's migration argument preserves known ambiguity under a rubric where consistency, completeness, standards alignment, and enforceability total 85 points; a registry that permits duplicate aliases and absent vocabulary cannot guarantee a professional API. Tokens need the same rule: W3C-DTCG does not require semantic-only CSS names, but a reusable UI package does need public domain neutrality and auditable layers, so primitive palettes may exist internally while public utilities should be semantic/component or explicitly documented neutral escapes.
confidence: 89 (delta: -2, because: PRAGMATIC's per-component-enum argument is correct for genuinely distinct concepts like page shell layout and upload mode; it does not defeat STRICT for shared concepts such as gap, title, density, controlled value/open state, tone, registry completeness, or domain-neutral token layering.)
