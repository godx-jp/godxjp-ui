# SKEPTIC · R1

No favourite. Read the topic, Bus and all four opening dossiers; R0 blindness applies to the advocates, not this mandated R1 rebuttal. References `ADV-*.md` below mean files in this directory. No runtime acceptance is claimed.

**Assumptions:** the fixed rubric and hard constraints remain binding; existing specs are commitments, not proof of implementation; the shared tree can change during review. Historical incidents below are documented accounts, not reproduced failures. Counterexamples are proposed falsification cases, not allegations that current implementations fail. **Confidence: 94/100** that none of the four has yet demonstrated protection against false acceptance; initial assessment, no position change. This is not a ranking or a probability that every implementation will fail.

## A — Ledger-driven capability parity

**Steelman.** A adds a reverse upstream inventory, reviews behavior rather than names, preserves explicit deferrals, and reserves shared files for integration. It does not merely count 127 local entries (`ADV-A.md:37-45,57-61`).

**Attack.** Its protection still ends at a reviewer promising to maintain the ledger. The adopted format says `RENAMED` means “no work,” while A itself acknowledges Tabs default differences: classification cannot certify equivalence (`docs/roadmap/antd-parity.md:33`; `ADV-A.md:69`). The reverse inventory fixes the obvious missing-component denominator, but who detects an omitted behavior inside an enumerated component? No completed inventory or omission challenge is supplied. A concedes this at `ADV-A.md:75`; coverage 20/25 and correctness 23/25 remain **[unsupported] as predicted outcomes**, even though labelled judgments (`:83-84`).

**Wrong-but-present challenge.** Let the ledger cite Cascader's selection API and a successful search journey while parent selection and deep hover remain broken. That is exactly the documented prior failure: parent could not drill, hover died at depth 3, and nested buttons produced a console error after only the search card was tested (`.claude/skills/godxjp-ui-component/SKILL.md:143-150`). Show the independent reviewer rejecting that evidence, rather than another template saying they should.

**Recurrence/cost.** Bilateral catalog review addresses the shape of commit `8365bf05`, but handwritten ownership evidence can still rot. Group serialization cannot cover Tree's cross-group utility move without the additional leases A proposes (`docs/roadmap/tree-components.md:129-132`). One integrator and one validation slot would constrain writes/load **if enforced**; acknowledgments, handoff protocol and service capacity are **[unsupported]**. Neither reviewer time per capability nor the shared-file queue is priced (`ADV-A.md:57-61`). A has no measured weekly throughput claim; do not infer one from “already in motion.”

## B — Consumer-screen-driven parity

**Steelman.** B inventories routes, roles and states, scans source for abandoned flows, keeps existing commitments, and uses Platform as the acceptance environment already required by policy (`ADV-B.md:29-39`; `CLAUDE.md:6-10`).

**Attack.** A complete census of existing screens cannot enumerate capabilities those screens never expressed. B explicitly admits both this and the abandoned-screen bias (`ADV-B.md:55-57`). Access and representativeness are unverified (`:20-21`). This changes the coverage promise; it does not satisfy “không miss” by cheaper means. Source scanning mitigates abandoned implementations, not needs that never produced source.

**Wrong-but-present challenge.** A Platform Cascader search screen can pass while the exported `changeOnSelect`/deep-hover modes fail elsewhere—the historical failure above. Likewise, testing ordinary Enter in ChatComposer does not prove Japanese/Vietnamese IME safety, an explicit local requirement (`docs/roadmap/ai-chat-components.md:124-126`). B's broader state matrix is a proposal, not evidence that all public modes are reachable through its fixtures. Correctness 90/100 is **[unsupported] as an outcome** (`ADV-B.md:64`).

**Recurrence/cost.** Commit `8365bf05` records a consumer following Topbar's catalog exactly and getting the wrong composition; hunting private imports alone would miss that fully public-API misuse. B's screen review might catch it, but does not enforce reciprocal guidance after the next edit (`docs/roadmap/antd-parity.md:34-35`; commit `8365bf05`, `mcp/src/data/components.ts`). Day-one route inventory and day-five acceptance lack route/state counts, fixture preparation time and reviewer capacity; **[unsupported] as delivery estimates** (`ADV-B.md:35`). Its integrator and validation slot are useful proposed controls, not evidence that the six active agents have yielded ownership (`:37-39`).

## C — Wholesale surface port, prune later

**Steelman.** C offers centrally supported, discoverable compositions, reuses engines, preserves normalized names, and candidly prices pruning as migration rather than deleting files (`ADV-C.md:21-27,31-35,57`).

**Attack.** C is ineligible under unchanged constraints, not merely low-scoring: its distinguishing Ribbon/Prompts/Welcome exports deliberately fail admission (`ADV-C.md:31`; `docs/COMPOSITION-VS-COMPONENT.md:32-42`). A hypothetical policy change cannot purchase points under the fixed rubric. Even after such a change, the demand, avoided assembly cost and representative telemetry assumptions are all **[unsupported]** by its own account (`ADV-C.md:9-12`). TreeList must remain exported and Badge cannot be renamed: this repo already supplies concrete counterevidence to cheap pruning (`docs/roadmap/tree-components.md:119-125`; `docs/roadmap/badge-tag-chip-count.md:142-146`).

**Wrong-but-present challenge.** A funded list of props can still deliver a `virtual` switch that renders all rows or loses full-set position metadata. The local List contract explicitly requires windowed DOM plus full `aria-setsize`/`aria-posinset`; API availability alone would deserve the rubric's worse-than-absent penalty (`docs/roadmap/list-masonry.md`, “Required semantics” and “Definition of done”; topic `00-Topic.md:124`). Full-surface commitment supplies no evidence those behavioral combinations fit the acceptance budget.

**Recurrence/cost.** More catalog surfaces increase obligations without proving fewer one-sided instructions or partial passes. C's one runner addresses concurrency only; its rotating integrator needs an acknowledged ownership transfer to avoid two owners during rotation (`ADV-C.md:41-43`). No such mechanism is demonstrated. Its 20/25 coverage score credits a delivery commitment as though it prevented regression; it does not (`:67`). Days 2–4 and a day-five acceptance window have no measured slice sizes (`:39`). **[unsupported] as capacity estimates.**

## D — Contract-first executable parity

**Steelman.** D separates disposition from verification, independently inventories upstream keys, tests negative fixtures, checks types and destinations, and proposes invalidating evidence after changes. It explicitly rejects green-map-equals-parity (`ADV-D.md:25,31-43`).

**Attack.** The hard part is discovering what the checker must reject. The existing prop guard historically missed **460/1,019** declared props because comments defeated extraction; Flex's `pad`/`padRaw`/`gapRaw` shipped without catalog guidance (`scripts/check-mcp-prop-sync.mjs:81-91`). The example guard separately skips unresolved external surfaces and ignores `on*` attributes (`scripts/check-doc-prop-existence.mjs:25-41,195-196`). Those are historical/current blind spots, not proof D will repeat them—but they defeat any presumption that adding a gate makes coverage trustworthy.

**Wrong-but-present challenge.** Keep a valid prop, test path and green assertion that only checks rendering; break the runtime callback in an unexercised mode. D's schema can remain consistent. Its proposed evidence invalidation must follow shared utilities, styles, tokens and dependency changes, not just the mapped component; otherwise acceptance outlives its premises. The dependency boundary and semantic rejection demonstration are **[unsupported]** (`ADV-D.md:40,43`). A maintainer approving another `verified` flag can reproduce Cascader's self-certified partial pass.

**Recurrence/cost.** D most directly proposes structural detection of `8365bf05`-style missing links, but cannot certify that linked advice is correct. Two days must cover inventory, schema, type resolution/adapters, negative fixtures and coordination; the estimate is explicitly **[unsupported]** (`ADV-D.md:12,37-43,55-58`). Reporting a missed deadline is not a fallback for paused repairs. One coordinator simultaneously owns the checker, map and nearly every shared integration file; no queue/capacity evidence supports that schedule. A validation slot still needs enforcement independent of the parity checker.

## What all four get wrong

**Steelman of the emerging common position:** combine discovery, explicit exceptions, behavioral evidence and serialized integration. Each dossier proposes these safeguards. Their agreement is not evidence they work.

- **The historical failures are different.** `8365bf05` corrected Topbar teaching Buttons while TopbarItem claimed replacement, plus three other one-sided relationships. Flex's parser blindness is a separate incident. Cascader's partial acceptance is a semantic failure. Load past 90 came from parallel full-suite workers (`CLAUDE.md:26`). A/B propose human review; C enlarges the obligations; D checks declared relationships. None demonstrates all three protections—catalog truth, complete behavioral acceptance, and enforced machine-wide scheduling.
- **Literal Ant parity is the wrong goal under the binding decisions.** ChatBubble deliberately excludes shadows and makes radius a token; CheckableTag is Toggle; visual knobs use tokens (`docs/roadmap/ai-chat-components.md:50,56`; `docs/roadmap/antd-parity.md:9-21`). A/B/D acknowledge adaptation and C also excludes surfaces. Therefore require a fixed capability-accountability denominator plus separate delivery/acceptance counts; never present name/prop matching as JP-enterprise quality. Platform acceptance remains mandatory, but “first consumer” does not establish representative demand (`CLAUDE.md:6-10`).
- **Even approved ledgers contain unevidenced economics.** “Every consumer has at least one long list” and “Small; broadly useful” are C7 assertions without consumer/bundle measurements (`docs/roadmap/list-masonry.md:31,98`). Preserve approved scope; do not count approval as empirical proof. The label “127 components” measures entries, not modes, interactions or review hours (topic `00-Topic.md:30`; `ADV-A.md:37`).
- **The forced choice hides the operational question.** A/B choose discovery/priority, C chooses admission breadth, D chooses enforcement. Making one primary does not establish who may mark a behavior accepted or who can stop another writer. All proposed leases need acknowledgments, a conflict/expiry rule, and a machine-wide validation owner; six agents and unrelated dirty work make a bare commit SHA insufficient to identify the tested artifact (topic `00-Topic.md:48-51`). This is a risk inference, not an observed collision during this round.

## The cheapest thing that would actually reduce risk

**Proposal, not a fifth strategy:** before trusting any completion score, require one independent reviewer to challenge one existing partial-pass case—Cascader parent selection and depth-three hover, including console output—and one catalog case from `8365bf05`. Record exact file-content identity, every required mode, terminal outcome, and failures/UNTESTED separately. Apply the existing no-partial-pass rule instead of inventing a universal new certification scheme (`.claude/skills/godxjp-ui-component/SKILL.md:143-150`). Have current writers acknowledge exact shared-file ownership and a single validation slot before integration; an unacknowledged handoff stays queued.

This is the smallest bounded risk-reduction probe proposed here, **not a measured cheapest-cost claim**. It tests whether the promised reviewer and scheduling controls exist and whether an attractive pass survives a known counterexample. Run it only in the later authorized implementation/acceptance phase; this debate ran no builds, tests or servers. Preserve this dissent even if the Judge selects a primary strategy: no credit for correctness, complete coverage or enforced serialization merely because the chosen plan promises them.


## R2 answer

**Steelman.** With exclusive ownership assumed, rejecting every imperfect safeguard evades the actual decision: which remaining risk is tolerable this week? That criticism is correct (`01-Bus.md:150-155`).

**Direct answer.** I accept **A's manual drift exposure** this week. B deliberately leaves unseen capabilities outside its discovery denominator; D requires a checker before repairs without demonstrating the incremental semantic protection that pays for that delay (`00-Topic.md:85-111`; `03-Reports/JUDGE-r2-questions.md:19-25,31-37`). This is a comparative judgment, not measured throughput evidence. My R1 refusal to choose was insufficient; I change it to a conditional preference for A.

**Correction.** I overstated D's distinctive protection against the catalog incident: an existing guard already checks reciprocal mentions, though not advice correctness. Withdraw any implication that A/B currently have no automated relationship detection (`03-Reports/JUDGE-r2-questions.md:11`). That correction does not resolve the names-intact, advice-wrong counterexample.

**Most exposed survivor: B**, specifically for wrong-but-present modes outside Platform fixtures. Search can succeed while parent drilling and depth-three hover fail: the documented Cascader incident demonstrates that mechanism (`.claude/skills/godxjp-ui-component/SKILL.md:143-148`). A's capability inventory at least creates a place to demand those cases; D separates consistency from behavioral verification (`02-Research/SKEPTIC.md:9,23,39-43`). This ranking is an inference; comparative failure rates are **[unsupported]**, and A/D can reproduce the same partial pass.

**Change and reversal threshold — proposed, not executed.** Require manual reopening of affected acceptance evidence after component, dependency, style or catalog edits; identify exact file contents and demand terminal outcomes for every declared mode plus console output before reacceptance. Missing evidence remains UNTESTED, never accepted (the existing forcing rule: `.claude/skills/godxjp-ui-component/SKILL.md:143-150`). If **one** stale accepted row survives an independent Cascader-mode or names-intact bad-advice challenge, withdraw A's manual-control preference. Switch to D only if its prerequisite demonstrably rejects that same escape and a measured bootstrap/integration budget fits the remaining week; otherwise block the affected acceptance, without pretending B solves it. These are decision thresholds, not proven detection or capacity claims.

**Assumptions.** Ownership is acknowledged as stipulated, not verified here; fixed scope and constraints remain; reviewers can withhold acceptance; unseen exported modes matter. Reviewer availability, manual dependency completeness and weekly capacity remain **[unsupported]**. No implementation, tests or acceptance probes ran.

**Final confidence:** **90/100, delta −4 from R1's 94**, on the same proposition that no option has demonstrated protection against false acceptance: the Judge's existing-guard correction weakens my blanket critique, while its semantic limitation preserves the central concern. Separately, **70/100 initial confidence** in choosing A's residual risk; R1 had no option preference, so an option-confidence delta is not defined. Preserve dissent: selecting A does not establish complete coverage or correct shipped behavior (`00-Topic.md:123-124`).
