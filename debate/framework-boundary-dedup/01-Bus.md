# 01 — Message Bus (append-only)

---

### r0 · ADV-LEAN → all · opening

claim: Option LEAN is the right boundary: keep domain-neutral primitives/layout/a11y interactions in core, extract app preference pickers, CountrySelect, AppProvider/formatDate/i18n, app-shell chrome, FilterBar, and query/router helpers to `@godxjp/ui-app`, and merge duplicate concepts such as Stack+Inline into Flex, ChoiceField into Field, shaped Skeletons into Skeleton recipes, and status dictionaries out of Badge.
evidence: Locale/Date/Time/Timezone pickers import AppProvider/i18n/app option catalogs and throw without app context (`src/components/navigation/locale-picker.tsx:2-4`, `:20-28`; `src/components/navigation/date-format-picker.tsx:2-4`, `:27-34`; `src/components/navigation/time-format-picker.tsx:2-4`, `:27-34`; `src/components/navigation/timezone-picker.tsx:3-5`, `:29-37`); AppProvider owns persisted locale/timezone/date-time preferences, request headers, i18n and datetime singleton sync (`src/app/app-provider.tsx:73-88`, `:105-130`, `:185-205`, `:213-245`); query helpers type against TanStack Query and React Router (`src/props/components/query.prop.ts:3-10`, `:14-26`, `:88-94`; `src/components/query/prefetch-link.tsx:1-4`, `:12-45`); Topbar/Sidebar/PageContainer encode product/project/admin chrome and router breadcrumbs (`src/components/layout/topbar.tsx:20-35`, `:36-71`; `src/components/layout/sidebar.tsx:169-178`, `:181-207`; `src/components/layout/page-container.tsx:1-2`, `:40-79`); Stack and Inline are one-div gap wrappers (`src/components/layout/stack.tsx:7-12`, `src/components/layout/inline.tsx:7-12`). Peer fact: Radix Primitives describes itself as a low-level accessible primitive layer for patterns like accordion/checkbox/combobox/dialog/select/slider/tooltip, Base UI as foundational unstyled/headless components, and React Aria as style-free accessible components designed for composition/custom patterns; none ship app preference pickers, CountrySelect, AppProvider request-header/i18n singleton, TanStack Query state widgets, or product app-shell chrome.
warrant: These components are app-runtime adapters, data/router integrations, fixed option-set widgets, or product/admin chrome over primitives; keeping them in core makes the framework less reusable, increases dependency surface, and violates the stated domain-neutral/no-wrapper rule, while extraction preserves consumer value through `@godxjp/ui-app` without polluting primitive boundaries.
assumptions: Core consumers should be able to use `@godxjp/ui` without adopting GODX locale catalogs, request-header conventions, React Router, TanStack Query, or product shell structure; `@godxjp/ui-app` may depend on those; peer primitive libraries are better boundary comparators than full enterprise app kits.
confidence: 91

### r0 · ADV-SQ → all · opening

claim: The right boundary is STATUS-QUO: keep the broad single `@godxjp/ui` framework, extract nothing, and merge only strict duplicate concepts such as Stack/Inline, Autocomplete/SearchSelect, Cascader/TreeSelect mode, Skeleton variants, StatusBadge/Badge, CardStat/StatCard, PageInset/PageContainer, and thin query wrappers.
evidence: AppProvider centralizes locale/timezone/date/time preferences, persistence, request headers, and formatter/i18n sync (`src/app/app-provider.tsx:73`, `:105`, `:185`, `:192`); preference pickers are Select wrappers with controlled/provider fallback (`src/components/navigation/locale-picker.tsx:20`, `src/components/navigation/timezone-picker.tsx:33`, `src/components/navigation/date-format-picker.tsx:37`, `src/components/navigation/time-format-picker.tsx:37`); app chrome is generic slot/layout structure (`src/components/layout/app-shell.tsx:8`, `src/components/layout/sidebar.tsx:169`, `src/components/layout/topbar.tsx:20`, `src/components/layout/page-container.tsx:18`); query helpers render lifecycle UI without app data (`src/components/query/data-state.tsx:26`, `src/components/query/infinite-query-state.tsx:30`); real duplicates are visible in same-shape one-div Stack/Inline (`src/components/layout/stack.tsx:7`, `src/components/layout/inline.tsx:7`), deprecated Autocomplete delegating to SearchSelect (`src/components/data-entry/autocomplete.tsx:14`, `:30`), private SkeletonBlock plus shaped skeleton family (`src/components/feedback/skeleton.tsx:8`, `:26`, `:44`, `:87`), and TreeSelect/Cascader sharing hierarchical-option selection (`src/components/data-entry/cascader.tsx:54`, `src/components/data-entry/tree-select.tsx:78`). Peer facts: Mantine ships AppShell/Flex/Stack/SimpleGrid/DatePicker; Ant ships Layout/Sider/Header/Cascader/TreeSelect/Calendar/DatePicker/Statistic/Badge/Skeleton/ConfigProvider; MUI ships AppBar/Container/Badge/Card/Skeleton and MUI X Date Pickers/Data Grid; Carbon ships Header/SideNav/Grid/DataTable/DatePicker/Skeleton states; Radix/shadcn ship primitives like Select, Collapsible, Dialog, AlertDialog, Sheet, Calendar.
warrant: These components are UI affordances and framework services, not business workflows; forcing them into an app package would trade a small purity gain for duplicated consumer code, broken one-package ergonomics, extra migration surface, and arbitrary boundary policing. Where the source shows strict same-concept overlap, merging improves coherence without deleting consumer value.
assumptions: Core may expose organized subpaths such as `query` or `admin`; "domain-neutral" allows standardized UI preferences like locale, timezone, country, filters, and query lifecycle states; current 7.0.0 rename churn is ignored; peer systems count both core packages and official adjacent packages as design-system evidence.
confidence: 83

### r0 · ADV-KIT → all · opening

claim: Option KIT is the right boundary: keep domain-neutral batteries-included UI/app-adjacent kit pieces (shell chrome, configurable preference selectors, filter/toolbars, query lifecycle helpers) while extracting hardcoded app/business data such as fixed locale/country/status mappings, and merge only true duplicate concepts such as Stack/Inline into a single flex primitive and CardStat/SkeletonCard naming into a stat family.
evidence: Repo: LocalePicker is just Select plus AppProvider fallback but hardcodes APP_LOCALES via `APP_LOCALES.map` ([src/components/navigation/locale-picker.tsx:20](src/components/navigation/locale-picker.tsx:20), [src/components/navigation/locale-picker.tsx:41](src/components/navigation/locale-picker.tsx:41), [src/app/types.ts:4](src/app/types.ts:4), [src/app/types.ts:29](src/app/types.ts:29)); TimezonePicker already accepts configured options and falls back to runtime IANA data ([src/components/navigation/timezone-picker.tsx:27](src/components/navigation/timezone-picker.tsx:27), [src/components/navigation/timezone-picker.tsx:39](src/components/navigation/timezone-picker.tsx:39), [src/app/timezones.ts:127](src/app/timezones.ts:127)); AppShell is slot-based page chrome ([src/components/layout/app-shell.tsx:8](src/components/layout/app-shell.tsx:8), [src/components/layout/app-shell.tsx:31](src/components/layout/app-shell.tsx:31)); Topbar currently includes product/project/search/notifications/tweaks assumptions that should become slots, not extraction of the shell concept ([src/components/layout/topbar.tsx:20](src/components/layout/topbar.tsx:20), [src/components/layout/topbar.tsx:36](src/components/layout/topbar.tsx:36), [src/components/layout/topbar.tsx:109](src/components/layout/topbar.tsx:109)); query helpers are isolated under `src/components/query` and render standard loading/error/empty/refetch affordances ([src/components/query/data-state.tsx:22](src/components/query/data-state.tsx:22), [src/components/query/query-refetch-button.tsx:11](src/components/query/query-refetch-button.tsx:11)); CountrySelect is country-domain option rendering over Select ([src/components/data-entry/country-select.tsx:23](src/components/data-entry/country-select.tsx:23), [src/components/data-entry/country-select.tsx:53](src/components/data-entry/country-select.tsx:53)); Badge hardcodes app/business statuses including assignment constants ([src/components/data-display/badge.tsx:40](src/components/data-display/badge.tsx:40), [src/components/data-display/badge.tsx:59](src/components/data-display/badge.tsx:59)). Peer facts: Mantine ships `AppShell` for header/navbar/aside/footer layout (https://mantine.dev/core/app-shell/); MUI lists App Bar, Card, Badge, Skeleton, Stack, Grid, Container, Data Grid, Date and Time Pickers, and Tree View in its component catalog (https://mui.com/components/); MUI X ships DatePicker (https://mui.com/x/react-date-pickers/date-picker/); Ant ships Layout, Cascader, DatePicker, TreeSelect, Badge, Card, Skeleton (https://ant.design/components/overview/, https://ant.design/components/cascader/); Carbon lists UI shell, Data table, Date picker, Tag, Tile, Search, Select, Pagination (https://v10.carbondesignsystem.com/components/overview/).
warrant: The peer baseline is not primitives-only: mature kits ship app-frame, data-entry, layout, loading, and navigation conveniences when they are domain-neutral and configurable; the correct boundary is therefore concept/domain neutrality, not "is this app-adjacent." Current source problems are mostly hardcoded data, router/provider coupling, and overly specific props inside otherwise reusable concepts, so extracting whole concepts would throw away high consumer value and diverge from MUI/Mantine/Ant/Carbon practice.
assumptions: The 7.0.0 refactor can rename or split APIs later; verdicts judge component nature, not current app usage; `@godxjp/ui/query` may remain a documented subpath rather than the default barrel; "core" means the framework package may include curated subpackages, not every export must be a low-level primitive; hardcoded message catalogs and locale/status data count as app data even when the widget shape is reusable.
confidence: 86

### r1 · ADV-SQ → all · rebuttal

claim: Keep STATUS-QUO: one broad `@godxjp/ui` framework, no package extraction, and only merge true duplicate concepts. The clearly worth-doing duplicate merges even under SQ are `Stack`/`Inline` into one direction-aware layout primitive, `Autocomplete` into `SearchSelect`, `MutationFeedback` into query error feedback, `QueryRefetchButton` into Button/query helper API, `PageInset` into PageContainer/layout spacing, `StatusBadge`/status mapping into `Badge`, `CardStat` aliases into `StatCard`, `SkeletonCard` rename into `SkeletonStat` under the Skeleton family, and probably `Cascader` as a TreeSelect/hierarchical-select display mode if the API can preserve cascade-column behavior.
steelman: LEAN's strongest case is that a primitive-core package should not force consumers to inherit app runtime adapters: LocalePicker hardcodes a finite app locale catalog and throws without AppProvider or controlled props, AppProvider owns persistence/request headers/i18n/date-time singleton sync, Topbar has product/project/search/notification/tweaks assumptions, and query helpers expose TanStack Query/React Router vocabulary; issue context also says app layouts, i18n providers, locale switchers, and domain widgets rightly live in the app (`debate/framework-boundary-dedup/context/issue-82.md:5-9`). KIT's strongest case is more damaging to SQ because it keeps the valuable app-kit concepts but targets the real impurity: hardcoded locale/country/status data and router/provider coupling should become configurable seams while AppShell, query lifecycle, filters, and preference selectors stay as curated domain-neutral kit.
evidence: `LocalePicker` imports `APP_LOCALES`, falls back to AppProvider, throws without context/control, and maps fixed locale options (`src/components/navigation/locale-picker.tsx:2-4`, `:20-28`, `:41-45`); `APP_LOCALES` is `["vi","en","ja"]` and request header names are fixed (`src/app/types.ts:4-5`, `:29-32`); AppProvider persists locale/timezone/time/date format, builds request headers, and syncs i18n/datetime globals (`src/app/app-provider.tsx:73-88`, `:105-130`, `:185-205`, `:213-245`); Topbar exposes product/project/search/notification/tweaks props (`src/components/layout/topbar.tsx:20-34`, `:36-70`, `:109-140`), but AppShell itself is generic region composition (`src/components/layout/app-shell.tsx:8-18`, `:31-40`); DataState is explicitly a query lifecycle widget and renders skeleton/error/empty/success without app records (`src/components/query/data-state.tsx:22-24`, `:44-57`); strict duplicates/thin wrappers are direct in source: `Stack` and `Inline` are one-div gap wrappers (`src/components/layout/stack.tsx:7-12`, `src/components/layout/inline.tsx:7-12`), `Autocomplete` is deprecated as a thin `SearchSelect` wrapper (`src/components/data-entry/autocomplete.tsx:11-17`, `:30-41`), `MutationFeedback` gates pending/error then returns `AlertQueryError` (`src/components/query/mutation-feedback.tsx:13-30`), `QueryRefetchButton` is a Button that calls `query.refetch()` and disables on `query.isFetching` (`src/components/query/query-refetch-button.tsx:11-40`), `PageInset` is a single `ui-page-inset` wrapper (`src/components/layout/page-inset.tsx:1-16`), Badge owns status-to-tone/icon/label mapping including assignment constants (`src/components/data-display/badge.tsx:40-62`, `:92-104`), StatCard already owns KPI tile semantics (`src/components/data-display/card.tsx:139-151`, `:169-216`), and SkeletonCard is documented as a stat-card/dashboard-tile skeleton (`src/components/feedback/skeleton.tsx:88-96`). Peer facts: Mantine ships AppShell; Ant ships Layout, Cascader, TreeSelect, DatePicker, Badge, Card, Skeleton, and ConfigProvider; MUI ships AppBar/Container/Badge/Card/Skeleton plus MUI X Date Pickers/Data Grid; Carbon ships UI shell/Header/SideNav/DataTable/DatePicker/Skeleton states; Radix/shadcn ship low-level primitives but are not the only valid UI-framework baseline.
warrant: LEAN proves several current APIs are too coupled, but it does not prove their concepts are non-framework by nature. Locale, timezone, date/time display preferences, shell regions, filter toolbars, async/query lifecycle states, and prefetching links are reusable UI affordances when made configurable and isolated under subpaths. KIT's configurable-data critique should be accepted as API cleanup inside the same package, not as extraction: removing fixed catalogs/status dictionaries from component internals preserves framework coherence without making consumers rebuild common app-kit pieces. The duplicate merges above are different because their concepts truly overlap or are near-pass-through wrappers; merging them reduces surface area without deleting consumer value.
assumptions: "Core" may include curated subpaths such as `@godxjp/ui/query` and app-shell exports; package extraction is only justified by business workflow/domain ownership, not by a component being app-adjacent; hardcoded defaults can be replaced by caller-supplied options/status maps without moving the component out of `@godxjp/ui`; current mid-refactor line drift is ignored except where it changes whether a duplicate still exists.
confidence: 79 (delta: -4 since r0, because: KIT's specific argument that hardcoded locale/status/country data are app data inside otherwise reusable widgets is stronger than SQ's original "API polish" framing, and LEAN's issue-82 evidence shows at least some real consumers expect app layouts/i18n/locale switchers to be app-owned even while requesting more primitives.)

### r1 · ADV-KIT → all · rebuttal

claim: KIT remains the right boundary, tightened: keep domain-neutral kit concepts in core/subpaths (AppShell/AppBar/Page layout, configurable locale/timezone/date/time preference selectors, FilterToolbar, DataState/InfiniteQueryState as an optional query adapter, TreeSelect/Cascader, DatePicker/Calendar, Skeleton family), but extract app data and policy seams that LEAN correctly identified: hardcoded locale/catalog/status/country dictionaries, request-header synchronization, React Router breadcrumb/link coupling, product/project-specific Topbar props, and thin same-concept wrappers.
steelman: LEAN's strongest case is not aesthetic minimalism; it is dependency sovereignty. Core currently forces or leaks AppProvider, GODX locale catalogs, request headers, React Router, and TanStack Query into components whose rendered UI is mostly Select/Button/Alert/Layout composition, so a consumer cannot adopt the framework without inheriting app-runtime assumptions. LEAN is right to demand extraction of CountrySelect, fixed APP_LOCALES/message catalogs, status dictionaries, request-header/i18n singleton policy, React Router breadcrumb links, product/project Topbar assumptions, Stack/Inline as separate wrappers, QueryRefetchButton/MutationFeedback as standalone names, and any GODX page-shape query flattening. SQ's strongest case is consumer value and migration cost: the package is already a broad framework, issue-82 says consumers missed low-level primitives while also valuing v6's higher-level direction (`debate/framework-boundary-dedup/context/issue-82.md:5`, `:47-49`), and peer kits do ship shells, layout, data-entry, date, table, skeleton, badge/tag, and navigation families, so extraction-only purity would make consumers rebuild repeated UI affordances.
evidence: AppProvider owns persisted locale/timezone/date/time preferences, request headers, i18n sync, and datetime singleton sync (`src/app/app-provider.tsx:73-88`, `:105-130`, `:185-205`, `:213-245`), so LEAN wins on extracting request/header/catalog policy. LocalePicker hardcodes `APP_LOCALES` while otherwise being Select plus context fallback (`src/components/navigation/locale-picker.tsx:20-28`, `:30-47`); TimezonePicker already shows the KIT-compatible shape by accepting `options` and controlled props before context fallback (`src/components/navigation/timezone-picker.tsx:21-37`, `:39-58`). AppShell is pure slot chrome (`src/components/layout/app-shell.tsx:8-18`, `:31-40`), while Topbar bakes product/project/search/notification/tweaks props (`src/components/layout/topbar.tsx:20-35`, `:36-70`, `:109-140`) and PageContainer bakes React Router Link into breadcrumbs (`src/components/layout/page-container.tsx:1-2`, `:40-79`), so the shell concept stays but those assumptions become slots/adapters. Query helpers explicitly type TanStack Query and React Router (`src/props/components/query.prop.ts:3-10`, `:14-26`, `:88-94`) while DataState renders generic skeleton/error/empty/success UI (`src/components/query/data-state.tsx:22-57`), so keep as `@godxjp/ui/query` or adapter package, not root primitive. CountrySelect renders flag/native-name/country-code policy over Select (`src/components/data-entry/country-select.tsx:23-33`, `:53-87`) and Badge hardcodes business statuses including assignment constants (`src/components/data-display/badge.tsx:40-62`, `:92-99`), so extract those dictionaries. Stack and Inline are one-div gap wrappers (`src/components/layout/stack.tsx:7-12`, `src/components/layout/inline.tsx:7-12`); QueryRefetchButton is a Button wrapper over `query.refetch()` (`src/components/query/query-refetch-button.tsx:11-40`); MutationFeedback is mostly an AlertQueryError gate (`src/components/query/mutation-feedback.tsx:13-30`). Peer facts: Mantine ships AppShell; MUI lists App Bar, Container, Grid, Stack, Badge, Card, Skeleton, Data Grid, Date/Time Pickers, and Tree View; Ant ships Layout, Cascader, TreeSelect, DatePicker, Badge, Card, Skeleton; Carbon lists UI shell, Data table, Date picker, Search, Select, Pagination, Tag, Tile.
warrant: The decisive line is nature plus configurability, not whether a component is app-adjacent. A shell, filter toolbar, timezone selector, query state boundary, table skeleton, or hierarchical selector is a reusable UI affordance when its data, labels, router, and runtime policy are injected; a fixed country/status/locale catalog or request-header synchronizer is app policy. LEAN over-corrects by treating every adapter-like component as business code even when the concept is a standard reusable kit affordance. SQ under-corrects by treating hardcoded app data and concrete integration dependencies as mere polish, which violates the domain-neutral rule and preserves avoidable dependency surface.
assumptions: Core may contain curated optional subpaths, but the root/default export should not force TanStack Query or React Router types; app-facing convenience adapters may live in `@godxjp/ui/query` or `@godxjp/ui-app` depending on dependency policy; peer systems count only as evidence of concept membership, not as permission to hardcode GODX catalogs; current 7.0.0 source details are evidence of coupling but the verdict targets future component nature.
confidence: 84 (delta: -2 since r0, because: LEAN's specific argument that AppProvider currently synchronizes request headers and module singletons, and that query/link props import TanStack Query plus React Router, moved me from "keep these in core with cleanup" to "keep the UI concepts only behind optional adapter/subpath boundaries and extract the runtime policy")

### r1 · SKEPTIC → all · rebuttal

claim: NONE of LEAN, KIT, or STATUS-QUO is coherent as stated. The correct concept boundary is a narrower fourth path: keep core UI concepts when they are domain-neutral, remove or privatize near-zero wrappers, fix hardcoded data as props/slots, and extract only concrete runtime adapters that force foreign providers/routers/query clients. Biggest risks: LEAN creates a second package that consumers must version-sync without proving better reuse; KIT normalizes app-runtime coupling by calling it "curated"; STATUS-QUO preserves the largest dependency and policy surface and mistakes "consumer value" for "core membership."
steelman: LEAN strongest case: AppProvider, i18n singleton, React Router links, TanStack Query helpers, product chrome, and hardcoded option widgets are not primitive UI and violate domain-neutral/no-wrapper rules. KIT strongest case: MUI/Mantine/Ant/Carbon prove mature design systems ship app shells, layout, data-entry, skeletons, date pickers, and other app-adjacent conveniences, so the fix is to make data configurable rather than amputate useful concepts. STATUS-QUO strongest case: package ergonomics matter; a single framework with organized subpaths can preserve consumer value while merging only strict duplicates.
evidence: LEAN over-extracts data-list widgets: LocalePicker's hard problem is `APP_LOCALES.map`, not the Select concept (`src/components/navigation/locale-picker.tsx:20-27`, `:41-44`); TimezonePicker already accepts caller/configured options before resolving labels (`src/components/navigation/timezone-picker.tsx:27-40`, `:55-58`); CountrySelect accepts `options` and is mostly `CountryOptionLabel` plus Select rendering (`src/components/data-entry/country-select.tsx:23-33`, `:53-82`). KIT/SQ under-extract runtime adapters: PrefetchLink imports both `react-router-dom` and TanStack Query and prefetches on hover/focus (`src/components/query/prefetch-link.tsx:2-3`, `:12-14`, `:27-43`); PageContainer imports React Router Link and is documented as mandatory admin-page shell (`src/components/layout/page-container.tsx:1-2`, `:40-50`). Some duplicate claims are false merges by nature: Cascader stores path arrays, active columns, `changeOnSelect`, and hover/click expansion (`src/components/data-entry/cascader.tsx:37-52`, `:60-83`, `:102-123`, `:147-210`), while TreeSelect stores flat selected keys, expanded tree keys, check strategy, and renders a `role="tree"` panel (`src/components/data-entry/tree-select.tsx:59-76`, `:83-104`, `:141-147`, `:197-216`); Dialog mode switches between Radix Dialog and AlertDialog semantics (`src/components/feedback/dialog.tsx:20-44`, `:88-106`), while Sheet is a directional slide-over with side variants (`src/components/feedback/sheet.tsx:7-8`, `:39-53`, `:63-86`). Stack and Inline are textually thin (`src/components/layout/stack.tsx:7-12`, `src/components/layout/inline.tsx:7-12`), but the audit itself notes Inline is a legitimate horizontal wrapping cluster name while Stack is a vertical sibling (`debate/framework-boundary-dedup/context/audit-findings.md`, Inline entry); that supports shared implementation/props, not necessarily erasing both public concepts. Badge proves "domain-coupled" often means injectable data, not component extraction: `STATUS_MAP` hardcodes assignment constants, but the neutral Badge variants and icon/status rendering are separate concerns (`src/components/data-display/badge.tsx:25-33`, `:40-62`, `:83-104`). Peer facts cut both ways: Mantine ships AppShell and Ant ships Cascader/TreeSelect, supporting KIT against LEAN; Radix/shadcn keep Dialog, AlertDialog, and Sheet distinct, supporting the false-merge objection against SQ.
warrant: The options each confuse a different axis. Package extraction is not the same as conceptual cleanup: moving LocalePicker/CountrySelect to `@godxjp/ui-app` when options/statuses could be caller data creates a second distribution surface without eliminating the reusable UI shape. Conversely, configurability is not enough when the component imports a router, query client, singleton provider, or request-header/i18n runtime; those are adapters, not framework components. Finally, "merge duplicates" must respect interaction contracts: column cascade selection, tree checking, modal dialog semantics, alert dialog semantics, and directional sheets are different user models even when they share Popover/Dialog primitives internally.
assumptions: App-adjacent components may live in core only if they can be consumed without GODX-specific catalogs, provider state, router choice, or TanStack Query; `@godxjp/ui-app` must justify itself by isolating real runtime adapters, not by becoming a dumping ground for configurable selects; peer design-system facts show possible boundaries, not proof that any single peer's scope is correct for this package.
confidence: 78 (delta: +8 since r0, because: ADV-KIT's specific argument that TimezonePicker already has an `options` prop and LocalePicker's issue is a hardcoded catalog moved me away from blanket LEAN extraction; ADV-LEAN's specific router/query evidence for PageContainer/PrefetchLink moved me away from KIT/SQ keeping all app-adjacent helpers.)

### r1 · ADV-LEAN → all · rebuttal

claim: LEAN still wins: keep core to domain-neutral primitives/layout/a11y interactions, export genuinely general primitives such as Card, Badge-without-status-map, Calendar/DatePicker, Collapsible-with-value, Checkbox/Radio/Switch/Field, Skeleton base, Dialog/Sheet/Popover/Tooltip, and possibly repaired TreeSelect; extract AppProvider/formatDate/i18n app runtime, preference pickers, CountrySelect, app shell chrome, FilterBar, and TanStack/React-Router helpers to `@godxjp/ui-app` or integration subpackages; merge duplicate wrappers.
steelman: KIT's strongest case is that the peer baseline is broader than Radix: Mantine ships AppShell, Ant ships Layout/Cascader/TreeSelect, Carbon ships UI shell/DataTable/DatePicker, and MUI ships AppBar/Container/DataGrid/Date Pickers, so a practical UI framework can keep app-adjacent but configurable layout, filtering, skeleton, and async conveniences. SQ's strongest case is migration/ergonomics: a single package avoids forcing every consumer to rebuild locale/timezone/date-format controls, query lifecycle states, page chrome, and status badges from primitives; many current defects can be treated as API polish or dedup rather than extraction.
evidence: The topic's hard constraint says the library rule is domain-neutral and no redundant wrappers (`debate/framework-boundary-dedup/00-Topic.md:39-42`), while the migration issue explicitly distinguishes missing framework primitives from "app layouts, i18n providers, locale switchers and domain widgets" that belong in the app (`debate/framework-boundary-dedup/context/issue-82.md:5-9`). LocalePicker imports app locale data/context/i18n and throws without AppProvider or controlled handlers (`src/components/navigation/locale-picker.tsx:2-4`, `src/components/navigation/locale-picker.tsx:20-28`, `src/components/navigation/locale-picker.tsx:41-44`). AppProvider owns persisted locale/timezone/date/time preferences, request headers, i18n sync, datetime singleton sync, and live relative-format toggles (`src/app/app-provider.tsx:73-88`, `src/app/app-provider.tsx:105-130`, `src/app/app-provider.tsx:185-205`, `src/app/app-provider.tsx:213-245`). Topbar bakes product/project switchers, search shortcut, notifications, user slot, and tweaks affordance into the header surface (`src/components/layout/topbar.tsx:20-35`, `src/components/layout/topbar.tsx:36-71`, `src/components/layout/topbar.tsx:87-113`, `src/components/layout/topbar.tsx:117-140`). Query props import TanStack Query and React Router types directly (`src/props/components/query.prop.ts:3-10`, `src/props/components/query.prop.ts:14-26`, `src/props/components/query.prop.ts:88-94`), and PrefetchLink couples React Router Link to TanStack Query prefetch (`src/components/query/prefetch-link.tsx:1-4`, `src/components/query/prefetch-link.tsx:12-45`). Badge's base variants are generic (`src/components/data-display/badge.tsx:25-33`, `src/components/data-display/badge.tsx:64-80`), but its status map includes assignment/business constants (`src/components/data-display/badge.tsx:40-62`). Card is a neutral compound surface (`src/components/data-display/card.tsx:32-45`, `src/components/data-display/card.tsx:51-137`), while StatCard is explicitly a KPI/dashboard tile (`src/components/data-display/card.tsx:139-152`, `src/components/data-display/card.tsx:169-217`). Peer facts conceded: Mantine ships AppShell; Ant ships Layout, Cascader, TreeSelect, DatePicker, Badge, Card, Skeleton; Carbon ships UI shell/Data table/Date picker; MUI ships AppBar/Container/DataGrid/Date Pickers.
warrant: KIT proves that "not Radix-low-level" is not by itself extraction evidence, so LEAN should not reject Card, Badge, DatePicker, Calendar, Collapsible, TreeSelect, DataTable-like primitives, or strong layout primitives merely because they are styled or higher-level. But KIT and SQ overread peer breadth: peers that ship AppShell/AppBar/Layout still do not justify a GODX provider that syncs request headers and singleton i18n, a LocalePicker fixed to `APP_LOCALES`, a Topbar with product/project/tweaks semantics, or query widgets typed to TanStack/React Router inside the primitive package. Nature matters: generic interaction primitives stay; app-runtime adapters, option-set preference widgets, product chrome, status dictionaries, router/query integration, and near-zero duplicate wrappers leave or merge.
assumptions: Core may include styled primitives and advanced controls when they remain domain-neutral and dependency-light; `@godxjp/ui-app` can preserve batteries-included ergonomics for GODX/admin consumers; peer broad kits are evidence for keeping neutral AppBar/Layout-like primitives only after product-specific props are removed, not for keeping the current app shell implementation in primitive core.
confidence: 88 (delta: -3 since r0, because: KIT's specific peer argument that Mantine/Ant/Carbon ship AppShell/Layout/UI-shell concepts shows LEAN must concede some app-frame primitives can be legitimate when slot-only and domain-neutral)
