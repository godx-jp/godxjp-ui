# 01 — Message Bus (append-only, chronological)

Every message is one fenced envelope. Never rewrite earlier entries; only append.

---
```
### r0 · ADV-C → all · opening
claim:       Option C is best for 7.0.0 because a hard, no-alias breaking release is the only defensible moment to collapse naming debt, app-layer leakage, and missing primitive gaps into one migration instead of forcing consumers through serial hard renames.
evidence:    7.0.0 already has locked hard removals/renames and no deprecated aliases (debate/ui7-scope-vs-issue82/00-Topic.md:5, debate/ui7-scope-vs-issue82/00-Topic.md:32); the rubric explicitly values international naming/design philosophy and coherence (debate/ui7-scope-vs-issue82/00-Topic.md:36, debate/ui7-scope-vs-issue82/00-Topic.md:38); #82 says a production migrator had to vendor missing standard primitives (debate/ui7-scope-vs-issue82/context/issue-82.md:5) and lists Radix/community primitives including Avatar, Separator, Progress, Skeleton, Toggle, AspectRatio, Accordion, ContextMenu, HoverCard, Drawer, InputOTP, Combobox, PasswordInput (debate/ui7-scope-vs-issue82/context/issue-82.md:15, debate/ui7-scope-vs-issue82/context/issue-82.md:23, debate/ui7-scope-vs-issue82/context/issue-82.md:33); source confirms real design debt: PageContainer imports react-router Link (src/components/layout/page-container.tsx:2), Topbar bakes product/project/search/notifications/tweaks props (src/props/components/layout.prop.ts:110), Sidebar is driven by sections/product config (src/props/components/layout.prop.ts:87), Select overloads compound and data-driven APIs (src/components/data-entry/select.tsx:12), Upload mixes generic upload with avatar crop/draft variants (src/components/data-entry/upload.tsx:107), Dialog hides AlertDialog behavior behind mode (src/components/feedback/dialog.tsx:32), query helpers import TanStack and react-router types (src/props/components/query.prop.ts:3, src/props/components/query.prop.ts:10), and local canon requires concept-first props/name synchronization/no wrapper primitives (mcp/src/data/rules.ts:36, mcp/src/data/rules.ts:44, mcp/src/data/rules.ts:46). Design-system facts: MUI uses AppBar/Container, Chakra uses SimpleGrid, Mantine uses AppShell, Radix/shadcn split Dialog and AlertDialog and expose Select as a compound primitive, and mainstream systems do not put product locale/date-format/country widgets in core.
warrant:     These are not cosmetic renames: they remove router/data/app-domain coupling, align names with established systems, and fill primitives consumers already expect. Because aliases are forbidden, postponing the audit redesigns or #82 primitives creates multiple breaking migrations; Option C spends the unavoidable migration cost once and leaves a coherent v7 surface.
assumptions: 7.0.0 has enough implementation bandwidth; migration docs/codemods are part of the release; "bulk" means accepting source-supported audit changes while keeping names the audit itself marks standard; Sheet->Drawer is an intentional Option C taxonomy choice even though #82 prefers Sheet and Drawer distinct; production consumers prefer one larger migration over repeated major/minor churn.
confidence:  82
```
```text
### r0 · ADV-A → all · opening
claim:       Option A is best for 7.0.0 because this release should spend its no-alias breaking-change budget only on locked cleanup and truly canonical names, not on audit-driven renames that hide broad API redesigns or on additive primitives that can ship safely in 7.x.
evidence:    `KeyValueGrid` is a semantic `<dl>` detail grid and fits Ant Design `Descriptions` (`src/components/data-display/key-value-grid.tsx:1`, `src/components/data-display/key-value-grid.tsx:12`, `src/components/data-display/key-value-grid.tsx:19`, `src/components/data-display/key-value-grid.tsx:32`); `ProgressMeter` is a bounded progress bar and `Progress` is the Radix/MUI/Chakra/Mantine/Carbon/shadcn canonical name (`src/components/data-display/progress-meter.tsx:9`, `src/components/data-display/progress-meter.tsx:13`, `src/components/data-display/progress-meter.tsx:15`); `Sheet` is a Radix Dialog side panel with side variants and default right docking, while #82 requests bottom-sheet `Drawer` as distinct (`src/components/feedback/sheet.tsx:2`, `src/components/feedback/sheet.tsx:39`, `src/components/feedback/sheet.tsx:43`, `src/components/feedback/sheet.tsx:52`, `debate/ui7-scope-vs-issue82/context/issue-82.md:11`, `debate/ui7-scope-vs-issue82/context/issue-82.md:29`); the audit candidates are redesigns, not cheap renames: `PageContainer` embeds router Link breadcrumbs/header/body/footer (`src/components/layout/page-container.tsx:2`, `src/components/layout/page-container.tsx:18`, `src/components/layout/page-container.tsx:40`, `src/components/layout/page-container.tsx:76`, `src/components/layout/page-container.tsx:78`), `Topbar` bakes product/project/search/notifications/tweaks (`src/components/layout/topbar.tsx:20`, `src/components/layout/topbar.tsx:21`, `src/components/layout/topbar.tsx:22`, `src/components/layout/topbar.tsx:27`, `src/components/layout/topbar.tsx:117`, `src/components/layout/topbar.tsx:131`), `FilterBar` owns filter-state clear behavior and i18n (`src/components/navigation/filter-bar.tsx:10`, `src/components/navigation/filter-bar.tsx:12`, `src/components/navigation/filter-bar.tsx:16`, `src/components/navigation/filter-bar.tsx:21`, `src/components/navigation/filter-bar.tsx:24`), `Select` overloads compound Radix and data-driven modes (`src/components/data-entry/select.tsx:12`, `src/components/data-entry/select.tsx:18`, `src/components/data-entry/select.tsx:25`, `src/components/data-entry/select.tsx:213`), and `Upload` spans dropzone/button/picture/avatar/avatar-crop/draft/crop flows (`src/components/data-entry/upload.tsx:107`, `src/components/data-entry/upload.tsx:131`, `src/components/data-entry/upload.tsx:152`, `src/components/data-entry/upload.tsx:205`, `src/components/data-entry/upload.tsx:248`, `src/components/data-entry/upload.tsx:263`, `src/components/data-entry/upload.tsx:330`); #82 gaps are real but additive because the UI barrel lacks Avatar/Separator/Toggle/AspectRatio and base `SkeletonBlock` is private (`src/components/ui/index.tsx:1`, `src/components/ui/index.tsx:31`, `src/components/feedback/skeleton.tsx:8`, `src/components/feedback/index.ts:31`); package exports include many public category and deep subpaths, so extra hard renames create broad import churn (`package.json:13`, `package.json:24`, `package.json:32`, `package.json:40`, `package.json:48`, `package.json:92`, `package.json:224`).
warrant:     The repo evidence shows the locked renames improve canonical naming with small, explainable migrations, while the audit's additional names require consumers to rewrite layout, navigation, select, and upload APIs for debatable future design purity. Since #82's missing primitives are standard but additive, deferring them to 7.x preserves velocity without forcing them into the same high-risk, no-alias major.
assumptions: 7.0.0 will ship hard renames with no deprecated aliases; the production-consumer migration cost stated in the topic is real; new primitives can be added in minor releases; agreement among audit agents is not evidence; design-system naming facts are useful only when the current component's behavior matches the canonical concept.
confidence:  86
```

```
### r0 · ADV-B → all · opening
claim:       Option B is the best 7.0.0 scope because this breaking release should remove locked legacy names while also filling the P1 primitive gaps a real migrator hit, without turning the release into an audit-driven cosmetic/API overhaul.
evidence:    Locked cleanup is already decided (debate/ui7-scope-vs-issue82/00-Topic.md:5, debate/ui7-scope-vs-issue82/00-Topic.md:8). #82 reports production migration pain from missing standard primitives that had to be vendored locally (debate/ui7-scope-vs-issue82/context/issue-82.md:5) and lists P1 Avatar, Separator, Progress, generic Skeleton, Toggle/ToggleGroup, and AspectRatio (debate/ui7-scope-vs-issue82/context/issue-82.md:15, debate/ui7-scope-vs-issue82/context/issue-82.md:21). The repo currently exports shaped skeletons only, while `SkeletonBlock` is private (src/components/feedback/skeleton.tsx:8, src/components/feedback/index.ts:31); the public UI barrel has Sheet/Switch but no Avatar, Separator, Toggle, ToggleGroup, AspectRatio, Drawer, or generic Skeleton export (src/components/ui/index.tsx:1, src/components/ui/index.tsx:31). `ProgressMeter` is already a 0-100 progress bar, so locked `ProgressMeter→Progress` satisfies #82 Progress cleanly (src/components/data-display/progress-meter.tsx:3, src/components/data-display/progress-meter.tsx:9). #82 explicitly says Drawer is a bottom-sheet using vaul and distinct from existing Sheet (debate/ui7-scope-vs-issue82/context/issue-82.md:29); current Sheet wraps Radix Dialog and defaults to side="right" side-panel behavior (src/components/feedback/sheet.tsx:2, src/components/feedback/sheet.tsx:39, src/components/feedback/sheet.tsx:52). Design-system facts: Radix/shadcn expose Avatar, Separator, Toggle, ToggleGroup, AspectRatio, and Skeleton-style primitives; shadcn/ui keeps Sheet for side panels and Drawer for vaul-based drawers. Audit candidates like PageContainer→PageShell, ResponsiveGrid→SimpleGrid, Topbar→AppBar, FilterBar→FilterToolbar, Select redesign, and Upload split are medium redesign/cosmetic proposals rather than P1 migrator gaps (debate/ui7-scope-vs-issue82/context/audit-findings.md:5, debate/ui7-scope-vs-issue82/context/audit-findings.md:7, debate/ui7-scope-vs-issue82/context/audit-findings.md:10, debate/ui7-scope-vs-issue82/context/audit-findings.md:29, debate/ui7-scope-vs-issue82/context/audit-findings.md:23, debate/ui7-scope-vs-issue82/context/audit-findings.md:34).
warrant:     Additive P1 primitives improve standards/completeness with low consumer breakage, while hard-renaming every audit candidate spends the migration budget on speculative churn. Sheet→Drawer is especially wrong because it destroys a useful concept boundary: side panel versus vaul bottom sheet. A breaking release is the right time to make unavoidable canonical renames and remove duplicates, but not the right excuse to rename functioning components where the audit has not shown clear user value.
assumptions: I assume Option B P1 means Avatar, Separator, generic Skeleton, Toggle, ToggleGroup, AspectRatio, and Progress via the locked rename; I assume these can be implemented as thin Radix/shadcn-style wrappers under existing token/rule constraints; I assume P2/P3 #82 items remain backlog except that a distinct vaul Drawer may be added now only if deliverable; I assume no deprecated aliases because the topic states hard renames only.
confidence:  86
```
```
### r1 · SKEPTIC → all · rebuttal
claim:       None of A/B/C is a clean 7.0.0 scope. A is under-scoped because it knowingly ships a "standard framework" major with already-reported P1 primitive holes; B is the least dangerous but still blurs the release story by mixing hard cleanup with new primitive parity work that does not need a major; C is correctness-risk, not ambition, because it accepts redesign-api scope creep and the Sheet→Drawer rename despite #82 explicitly defining Drawer as a distinct bottom-sheet. The biggest risk in A is reputational/completeness failure; in B, scope ambiguity and dependency/API settling under a major deadline; in C, irreversible semantic breakage plus a deliverability collapse.
steelman:    A's strongest version is that hard no-alias renames are the only changes that actually require 7.0.0, while #82 additions are additive and can land in 7.x minors without breaking consumers; it correctly treats PageContainer, Topbar, FilterBar, Select, Upload, Sidebar, and AppShell audit items as redesigns, not "just names" (debate/ui7-scope-vs-issue82/02-Research/ADV-A.md:1). B's strongest version is that #82 is not speculative audit churn: a production migrator already vendored missing P1 Radix/community primitives, and thin Avatar/Separator/Skeleton/Toggle/AspectRatio wrappers can fill obvious framework gaps without breaking existing users (debate/ui7-scope-vs-issue82/context/issue-82.md:5, debate/ui7-scope-vs-issue82/context/issue-82.md:15). C's strongest version is that if the project has already chosen hard renames with no deprecated aliases, one coordinated migration can be cheaper than serial majors, and the audit has real evidence of router/query/app-domain coupling in UI exports (debate/ui7-scope-vs-issue82/00-Topic.md:32, src/components/layout/page-container.tsx:2, src/props/components/query.prop.ts:3, src/components/navigation/locale-picker.tsx:20).
evidence:    The topic says no deprecated aliases and real MF usage/migration cost are hard constraints (debate/ui7-scope-vs-issue82/00-Topic.md:32, debate/ui7-scope-vs-issue82/00-Topic.md:34). #82 explicitly lists P1 missing primitives and says they were vendored locally during production migration (debate/ui7-scope-vs-issue82/context/issue-82.md:5, debate/ui7-scope-vs-issue82/context/issue-82.md:15). #82 also says "Drawer — bottom-sheet (vaul); distinct from the existing side Sheet" (debate/ui7-scope-vs-issue82/context/issue-82.md:29), while current Sheet is a Radix Dialog side panel with `side` variants and default right docking (src/components/feedback/sheet.tsx:2, src/components/feedback/sheet.tsx:39, src/components/feedback/sheet.tsx:52). The public UI barrel lacks Avatar/Separator/Toggle/ToggleGroup/AspectRatio and generic Skeleton, while `SkeletonBlock` is private (src/components/ui/index.tsx:1, src/components/ui/index.tsx:31, src/components/feedback/skeleton.tsx:8, src/components/feedback/index.ts:31). The audit's redesign-api list is broad: PageContainer router-decoupling and shell split, ResponsiveGrid flexible responsive API, AppShell/Sidebar composition rework, Topbar AppBar slot rewrite, Select split, Upload/AvatarUpload split, Dialog/AlertDialog split, query helper rewrites, filter toolbar split, locale/date-format domain extraction (debate/ui7-scope-vs-issue82/context/audit-findings.md:5, debate/ui7-scope-vs-issue82/context/audit-findings.md:7, debate/ui7-scope-vs-issue82/context/audit-findings.md:8, debate/ui7-scope-vs-issue82/context/audit-findings.md:9, debate/ui7-scope-vs-issue82/context/audit-findings.md:10, debate/ui7-scope-vs-issue82/context/audit-findings.md:23, debate/ui7-scope-vs-issue82/context/audit-findings.md:34, debate/ui7-scope-vs-issue82/context/audit-findings.md:27, debate/ui7-scope-vs-issue82/context/audit-findings.md:29, debate/ui7-scope-vs-issue82/context/audit-findings.md:46). Design-system fact: shadcn/ui keeps Sheet for side panels and Drawer for vaul drawers; Radix/shadcn expose Avatar, Separator, Toggle, ToggleGroup, AspectRatio, Accordion, ContextMenu, HoverCard; MUI uses AppBar and Chakra uses SimpleGrid, but Carbon/Fluent more often say Header and MUI says Grid/Container, so those names are not universal proof by themselves.
warrant:     Sheet→Drawer is actually wrong for this library because it erases the exact distinction the production issue asks to preserve: side panel versus bottom-sheet. C's "broad sliding-panel family" move is [unsupported] by #82 and creates a future collision where `Drawer` must mean both Radix/Dialog side panel and vaul bottom sheet. The audit redesign-api items that are scope-creep for 7.0.0 are the ones requiring behavioral or architectural rewrites rather than canonical name repair: PageContainer→PageShell split/router decoupling, AppShell/Sidebar composition, Topbar→AppBar slot/domain cleanup, Select split, Upload/AvatarUpload split, Dialog/AlertDialog split, query-boundary rewrites, FilterBar→Toolbar split, Locale/DateFormat app-layer extraction, DataTable pagination changes, TreeSelect/Cascader ARIA/model changes. Those may be valid future work, but bundling them with no-alias locked cleanup multiplies migration surfaces and test burden. A fails in the other direction: because #82 is already production evidence, shipping a major that still lacks Avatar/Separator/generic Skeleton/Toggle/AspectRatio makes "internationally standard naming/design philosophy" look selectively applied. B is still not safe by default: adding P1 primitives in the breaking release can blur the 7.0.0 story from "remove/rename legacy surface" to "also start shadcn parity," which invites P2/P3 bikeshedding (Drawer? Accordion? Combobox?) and forces API/dependency choices on the same timeline as hard migration docs.
assumptions: Locked cleanup remains non-negotiable; "none" means none of the three described option envelopes, not "do nothing"; additive primitives can ship in 7.x without a major; if P1 primitives are added in 7.0.0, they must be explicitly frozen to P1 only and must not include Sheet→Drawer or P2/P3; audit findings are evidence only where source behavior and design-system precedent align.
confidence:  79 (delta: +0 since r0, because: no prior SKEPTIC r0 baseline exists; the specific rival argument that most constrained my skepticism is B's production-vendoring evidence for P1 primitives, but it moves me only to "B is least dangerous," not to endorsing B as the right 7.0.0 envelope)
```
```
### r1 · ADV-B → all · rebuttal
claim:       Option B still has the best 7.0.0 scope: ship the locked cleanup, keep `Sheet` as the side-panel primitive, add #82's P1 primitive gaps, and reject the audit's broad cosmetic/API overhaul for this release. `Drawer` may be added only as a distinct vaul bottom-sheet; it must not be the renamed `Sheet`.
steelman:    Strongest Option A: no-alias hard renames are the scarce 7.0.0 resource, while #82 P1 primitives are additive and can ship in 7.x without breaking anyone. A is right that `Sheet` must be kept, that `Descriptions`/`Progress` are the clearly canonical renames, and that forcing PageContainer/Topbar/Sidebar/Select/Upload migrations before a focused design pass would be churn. Strongest Option C: the audit is not just bikeshedding; several components really do leak router, product, query, and domain concerns, and a major release is the cleanest time to remove those leaks if implementation bandwidth exists. C is right that `PageContainer` importing router `Link`, `Topbar` owning product/project/search/notification/tweaks concerns, `Sidebar` taking product/sections config, `Select` mixing compound and data-driven APIs, and `Upload` bundling avatar crop flows are real design-system debts.
evidence:    #82 is explicit production evidence, not speculative audit consensus: a migrator from a shadcn/Radix-style shared UI had to vendor missing primitives locally (debate/ui7-scope-vs-issue82/context/issue-82.md:5) and asks for only framework-appropriate primitives while excluding app layouts/i18n/domain widgets (debate/ui7-scope-vs-issue82/context/issue-82.md:7). Its P1 list is Avatar, Separator, Progress, Skeleton, Toggle/ToggleGroup, and AspectRatio (debate/ui7-scope-vs-issue82/context/issue-82.md:15, debate/ui7-scope-vs-issue82/context/issue-82.md:21). The current library has shaped skeleton exports but the generic block is private (`src/components/feedback/skeleton.tsx:8`, `src/components/feedback/index.ts:31`) and the public UI barrel has no Avatar/Separator/Toggle/ToggleGroup/AspectRatio/generic Skeleton equivalents (`src/components/ui/index.tsx:1`, `src/components/ui/index.tsx:31`). `ProgressMeter` already matches the P1 Progress concept as a bounded progress component (`src/components/data-display/progress-meter.tsx:9`, `src/components/data-display/progress-meter.tsx:13`, `src/components/data-display/progress-meter.tsx:15`). #82 says `Drawer` is a bottom-sheet and distinct from existing side `Sheet` (debate/ui7-scope-vs-issue82/context/issue-82.md:29), while current `Sheet` is a Radix Dialog-based side panel with `side` variants and default right placement (`src/components/feedback/sheet.tsx:2`, `src/components/feedback/sheet.tsx:39`, `src/components/feedback/sheet.tsx:43`, `src/components/feedback/sheet.tsx:52`). The audit's strongest findings are real redesign work, not cleanup-level renames: `PageContainer` embeds router Link and page shell regions (`src/components/layout/page-container.tsx:2`, `src/components/layout/page-container.tsx:18`, `src/components/layout/page-container.tsx:40`, `src/components/layout/page-container.tsx:76`); `Topbar` bakes product/project/search/notifications/tweaks props (`src/components/layout/topbar.tsx:20`, `src/components/layout/topbar.tsx:21`, `src/components/layout/topbar.tsx:22`, `src/components/layout/topbar.tsx:27`, `src/components/layout/topbar.tsx:117`, `src/components/layout/topbar.tsx:131`); `Select` overloads compound and data-driven modes (`src/components/data-entry/select.tsx:12`, `src/components/data-entry/select.tsx:18`, `src/components/data-entry/select.tsx:25`, `src/components/data-entry/select.tsx:213`); `Upload` spans dropzone/button/picture/avatar/avatar-crop/draft/crop flows (`src/components/data-entry/upload.tsx:107`, `src/components/data-entry/upload.tsx:131`, `src/components/data-entry/upload.tsx:152`, `src/components/data-entry/upload.tsx:205`, `src/components/data-entry/upload.tsx:248`, `src/components/data-entry/upload.tsx:330`). Design-system facts: Radix/shadcn expose Avatar, Separator, Toggle, ToggleGroup, AspectRatio, Progress, and Skeleton-style primitives; shadcn/ui keeps `Sheet` for side panels and `Drawer` for vaul drawers; MUI uses `AppBar`, Chakra uses `SimpleGrid`, Mantine uses `AppShell`, but those names do not prove this repo's app-shaped components can be hard-renamed without redesign.
warrant:     A's deferral argument underweights the concrete #82 production pain: a "standard framework" that ships v7 without Avatar/Separator/Skeleton/Toggle/AspectRatio still leaves consumers vendoring canonical primitives even after a major cleanup. These additions are low-breakage because they add missing exports rather than changing existing ones, and `Progress` is already covered by the locked rename. C's one-big-migration argument overweights coherence: the audit debts are legitimate, but the evidence shows they require broad API design, not simple international-name correction. Renaming `Topbar` to `AppBar` while it still owns product/project/tweaks props would make the name more canonical and the abstraction more misleading; splitting `Select` or `Upload` would be a full consumer rewrite unrelated to #82 P1. `Sheet` -> `Drawer` is the clearest C failure because it contradicts the production issue's taxonomy and erases a useful side-panel/bottom-sheet boundary that shadcn users already understand.
assumptions: Option B's P1 scope remains Avatar, Separator, generic Skeleton, Toggle, ToggleGroup, AspectRatio, and Progress via `ProgressMeter` -> `Progress`; P2/P3 #82 items remain backlog except a separate bottom-sheet `Drawer` if deliverable; all hard renames have no deprecated aliases; audit-sourced API cleanups deserve a later focused major/minor plan only after component contracts and migration helpers are designed.
confidence:  84 (delta: -2 since r0, because: A's specific argument that P1 additions are additive and can ship in 7.x weakens the urgency of doing them in 7.0.0, and C's specific router/product/query-coupling evidence shows more audit findings are genuine debt than my r0 wording implied. Neither argument beats #82's production primitive gap or the Sheet/Drawer taxonomy evidence.)
```

```text
### r1 · ADV-A → all · rebuttal
claim:       Option A still wins: ship the locked cleanup plus the canonical Descriptions/Progress/StatCard-style changes, keep `Sheet`, reject the audit's extra hard renames/redesigns as no-alias churn, and schedule #82's additive primitives for 7.x minors instead of making them 7.0.0 exit criteria.
steelman:    Option B's strongest version is: #82 is not hypothetical wishlisting; a production migrator vendored standard P1 primitives, and Avatar/Separator/Skeleton/Toggle/ToggleGroup/AspectRatio are additive Radix/shadcn-style gaps with low breakage, so adding them in 7.0.0 improves completeness without audit churn (debate/ui7-scope-vs-issue82/context/issue-82.md:5, debate/ui7-scope-vs-issue82/context/issue-82.md:15, src/components/ui/index.tsx:1, src/components/ui/index.tsx:31, src/components/feedback/skeleton.tsx:8, src/components/feedback/index.ts:31). Option C's strongest version is: the audit is not merely cosmetic because source confirms router/data/app-domain coupling and hidden semantic forks, so a no-alias major is the only clean moment to fix PageContainer, Topbar, Sidebar/AppShell, Select, Upload, Dialog/AlertDialog, query helpers, and missing primitives in one migration (src/components/layout/page-container.tsx:2, src/components/layout/topbar.tsx:20, src/props/components/layout.prop.ts:87, src/components/data-entry/select.tsx:12, src/components/data-entry/upload.tsx:107, src/components/feedback/dialog.tsx:32, src/props/components/query.prop.ts:3).
evidence:    The topic says hard rename/no deprecated aliases and real consumer migration cost are constraints, not hypotheticals (debate/ui7-scope-vs-issue82/00-Topic.md:32, debate/ui7-scope-vs-issue82/00-Topic.md:34). Locked cleanup already breaks consumers across removals, merges, and renames (debate/ui7-scope-vs-issue82/00-Topic.md:5, debate/ui7-scope-vs-issue82/00-Topic.md:8). The current `Sheet` is a Radix Dialog side panel with side variants/default right docking, while #82 explicitly asks for vaul bottom-sheet `Drawer` distinct from existing `Sheet` (src/components/feedback/sheet.tsx:2, src/components/feedback/sheet.tsx:39, src/components/feedback/sheet.tsx:43, src/components/feedback/sheet.tsx:52, debate/ui7-scope-vs-issue82/context/issue-82.md:29); design-system fact: shadcn/ui keeps Sheet and Drawer distinct. The audit candidates themselves describe many changes as redesign-api, not rename-only: PageContainer needs router decoupling, Topbar needs slot composition/no product props, Sidebar needs compound sections/items, Select needs primitive-vs-data-driven split, Upload needs avatar/crop split, and Dialog needs AlertDialog separation (debate/ui7-scope-vs-issue82/context/audit-findings.md:5, debate/ui7-scope-vs-issue82/context/audit-findings.md:10, debate/ui7-scope-vs-issue82/context/audit-findings.md:9, debate/ui7-scope-vs-issue82/context/audit-findings.md:23, debate/ui7-scope-vs-issue82/context/audit-findings.md:34, debate/ui7-scope-vs-issue82/context/audit-findings.md:26). #82 P1 is real but additive: it asks to add missing primitives, not to rename existing exports, and `Progress` is already covered by locked `ProgressMeter` -> `Progress` (debate/ui7-scope-vs-issue82/context/issue-82.md:15, debate/ui7-scope-vs-issue82/context/issue-82.md:18, debate/ui7-scope-vs-issue82/00-Topic.md:8).
warrant:     B beats A on "7.0.0 feels complete" for P1 primitive parity; I concede that. But completeness is only 15 points, while migration cost/churn plus deliverability are 40 points, and additive primitives do not need a major. Making P1 primitives a 7.0.0 requirement increases release scope without reducing any no-alias migration pain; consumers can adopt Avatar/Separator/Skeleton/Toggle/AspectRatio in 7.1 exactly as safely as in 7.0. C correctly identifies real architecture debt, but its remedy converts medium audit findings into mandatory public API rewrites. Renaming `Topbar` to `AppBar` while also redesigning product/search/notification props, or `Select` while splitting abstraction levels, is not "spend the migration once"; it is a broad unproven replacement surface bundled with already-decided breaks. The worst C call is `Sheet` -> `Drawer`: it contradicts the actual requested taxonomy and burns the future bottom-sheet name for a side-panel implementation.
assumptions: New primitives can be added in any 7.x minor without breaking existing consumers; 7.0.0 implementation bandwidth and migration docs are finite; the stated production consumer cost matters more than audit-agent agreement; source-confirmed design debt should be queued into focused follow-up majors/minors only when the replacement API is proven; no deprecated aliases means every extra rename is immediate churn.
confidence:  82 (delta: -4 since r0, because: ADV-B's specific production-migrator argument for additive P1 primitive parity is stronger than my r0 framing of #82 as merely backlog; it improves coherence/completeness, but does not overcome the fact that additions can ship safely in 7.x while extra audit renames cannot be undone cheaply)
```
```text
### r1 · ADV-C → all · rebuttal
claim:       Option C remains the right 7.0.0 strategy, but it must be evidence-filtered maximalism: do the locked cleanup, add #82 primitives, and make the source-backed naming/design repairs in one no-alias major; drop audit items that are merely prettier vocabulary. I now carve out `Sheet`->`Drawer` as the biggest weak C item: keep `Sheet` for the Radix side panel and add a distinct vaul bottom-sheet `Drawer`, while still doing the broader PageShell/AppBar/Sidebar/Select/Dialog/Upload/query/app-domain redesigns in 7.0.0.
steelman:    A's strongest version: no-alias hard renames are expensive in a real production app, additions can ship in 7.x, and the audit mixes real design debt with broad redesigns; therefore 7.0.0 should spend breakage only on locked, obviously canonical cleanup. This is strongest on `Sheet`: #82 explicitly says side `Sheet` is already present and asks for bottom-sheet `Drawer` as distinct, while current `Sheet` is a Radix Dialog side panel with side variants, so `Sheet`->`Drawer` would break a clean concept boundary. B's strongest version: add P1 primitives now because #82 is concrete production pain, but reject broad audit churn because P1 additions are low-risk and hard renames are not; keep `Sheet`, satisfy `Progress` via the locked rename, and do not let medium-severity audit redesigns displace deliverable primitive parity.
evidence:    The release is already a hard breaking release with no deprecated aliases and real consumer migration cost (debate/ui7-scope-vs-issue82/00-Topic.md:32, debate/ui7-scope-vs-issue82/00-Topic.md:34). The rubric explicitly rewards standards/design fit and coherence/completeness, not just low churn (debate/ui7-scope-vs-issue82/00-Topic.md:38, debate/ui7-scope-vs-issue82/00-Topic.md:41). #82 reports a production migrator had to vendor missing primitives and lists P1/P2/P3 standard primitives, while deliberately excluding app-specific widgets from framework scope (debate/ui7-scope-vs-issue82/context/issue-82.md:5, debate/ui7-scope-vs-issue82/context/issue-82.md:7, debate/ui7-scope-vs-issue82/context/issue-82.md:15, debate/ui7-scope-vs-issue82/context/issue-82.md:23, debate/ui7-scope-vs-issue82/context/issue-82.md:33). Source-backed audit debt is not cosmetic: `PageContainer` imports `react-router-dom` `Link` and owns title/breadcrumb/body/footer shell behavior, so `PageContainer` is misleading and router-coupled (src/components/layout/page-container.tsx:2, src/components/layout/page-container.tsx:18, src/components/layout/page-container.tsx:40, src/components/layout/page-container.tsx:76); `Topbar` bakes product/project/search/notifications/tweaks/user into a supposed primitive (src/components/layout/topbar.tsx:20, src/components/layout/topbar.tsx:109, src/components/layout/topbar.tsx:117, src/components/layout/topbar.tsx:131); `Sidebar` is driven by `sections` plus product/brand chrome rather than composable items (src/props/components/layout.prop.ts:87, src/props/components/layout.prop.ts:90, src/components/layout/sidebar.tsx:169, src/components/layout/sidebar.tsx:181); `Select` is a union of compound and data-driven/search contracts (src/components/data-entry/select.tsx:12, src/components/data-entry/select.tsx:25, src/components/data-entry/select.tsx:191); `Dialog` hides alertdialog semantics behind `mode="confirm"` (src/components/feedback/dialog.tsx:19, src/components/feedback/dialog.tsx:32, src/components/feedback/dialog.tsx:88); `Upload` mixes generic upload with avatar crop/draft behavior (src/components/data-entry/upload.tsx:107, src/components/data-entry/upload.tsx:128, src/components/data-entry/upload.tsx:330); query helpers expose TanStack Query and router concerns in UI props (src/props/components/query.prop.ts:3, src/props/components/query.prop.ts:10, src/components/query/query-refetch-button.tsx:24). The rival Sheet argument is also evidence-backed: #82 says `Drawer` is bottom-sheet/vaul and distinct from existing `Sheet`, and current `Sheet` is a side-positioned Radix Dialog wrapper (debate/ui7-scope-vs-issue82/context/issue-82.md:11, debate/ui7-scope-vs-issue82/context/issue-82.md:29, src/components/feedback/sheet.tsx:2, src/components/feedback/sheet.tsx:39, src/components/feedback/sheet.tsx:52). Design-system facts: MUI uses `AppBar`, Chakra uses `SimpleGrid`, Mantine uses `AppShell`, Ant Design uses `Descriptions`, Radix/shadcn split `Dialog` and `AlertDialog`, shadcn keeps side `Sheet` separate from vaul `Drawer`, and mainstream systems do not put product locale/date-format/country widgets in core.
warrant:     A and B correctly identify migration cost, but they treat "not locked" as if it means "not urgent." That misses the no-alias economics: if `PageContainer`, `Topbar`, `Sidebar`, `Select`, `Dialog`, `Upload`, query helpers, and app-domain pickers are publicly wrong today, deferring them creates another hard migration later. The concrete source evidence shows those are real portability, semantics, and composition defects, not naming taste. The right rebuttal to A is that deferring #82 leaves known primitive gaps after a major that claims standardization; the right rebuttal to B is that P1-only parity fixes incompleteness but preserves the very public APIs that violate the library's own domain-neutral and concept-first canon. However, C should not blindly rubber-stamp the audit. Weak/cosmetic audit renames to drop even under C: `FormField`->`Field` because the audit calls the design correct and severity low; `FilterGroup`->`FilterSection` as a standalone rename, because the real fix is ARIA/compound toolbar grouping; `SkeletonCard`->`SkeletonStat` unless it is tied to the locked `StatCard` extraction, because shaped skeleton names are secondary once generic `Skeleton` exists; `PageInset`->`PageGutter`, because the better fix is merge/remove under page body gutters rather than preserve a renamed wrapper; `TimeFormatPicker`->`TimeFormatSwitcher` as a public core rename, because these app-preference components should leave core or become generic Select composition; `QueryRefetchButton`->`RefetchButton`, because the better fix is merge into Button/query pattern, not a shorter export; and any rename where the audit itself says the name is standard (`Inline`, `AppShell`, `Sidebar`, `Breadcrumb`, `DataTable`, `Timeline`, `SearchInput`, `Select`, `Dialog`, `Steps`, `TreeSelect`, `Upload`, `ColorPicker`, `Slider`, `Calendar`, `Radio`, `Collapsible`, `TimezonePicker`, `PrefetchLink`). Sheet->Drawer is no longer just "weak/cosmetic"; it is actively contradicted by #82 plus shadcn/vaul taxonomy, so C should keep `Sheet` and add `Drawer` distinct while still doing the maximal one-major overhaul elsewhere.
assumptions: Implementation bandwidth exists for a staged-but-single 7.0.0 migration with docs/codemods; "bulk audit" means evidence-backed repairs, not every line of the 70-agent list; additions of #82 P2/P3 can be included in the 7.0.0 program once APIs are settled; consumer pain from one planned large migration is lower than repeated hard renames across later majors/minors; the library wants a standard primitive layer and a domain-neutral core, not just fewer exports.
confidence:  76 (delta: -6 since r0, because: ADV-B/A's specific Sheet argument moved me: #82 explicitly distinguishes side `Sheet` from vaul bottom-sheet `Drawer`, and the current source is exactly a Radix side-panel Sheet, so my r0 `Sheet`->`Drawer` defense was weaker than the evidence.)
```
