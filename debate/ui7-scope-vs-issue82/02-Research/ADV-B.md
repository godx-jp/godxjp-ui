Position: choose Option B for 7.0.0: locked cleanup, keep `Sheet`, add the P1 primitive gaps from #82, and accept only renames that clearly improve standards without broad API churn.

- Accept locked removals: remove `ScanPanel`, `CodeBadge`, `ShellApp`, `Menu`, and `MobileFrame` because the topic says this cleanup is already decided, not under debate; no further justification is needed inside this round (debate/ui7-scope-vs-issue82/00-Topic.md:5).
- Accept `StatusBadge` -> `Badge(status)`: locked by the topic, and the current public data-display barrel still exports both `Badge` and `StatusBadge`, so 7.0.0 is the correct hard-rename point (debate/ui7-scope-vs-issue82/00-Topic.md:6, src/components/data-display/index.ts:1, src/components/data-display/index.ts:17).
- Accept `TabsItems` -> `Tabs(items)`: locked by the topic, and the admin barrel currently exports both `TabsItems` and `Tabs`, confirming a duplicated surface to collapse (debate/ui7-scope-vs-issue82/00-Topic.md:7, src/components/admin/index.ts:47).
- Accept `SwitchField` -> `ChoiceField` + `Switch`: locked by the topic; it also follows the repo rule that one concept should be represented once rather than duplicated (debate/ui7-scope-vs-issue82/00-Topic.md:7, mcp/src/data/rules.ts:36).
- Accept `KeyValueGrid` -> `Descriptions`: locked by the topic, and the implementation is a semantic `<dl>` detail-description grid, which matches the Ant Design `Descriptions` concept better than a generic key-value utility name (debate/ui7-scope-vs-issue82/00-Topic.md:7, src/components/data-display/key-value-grid.tsx:1, src/components/data-display/key-value-grid.tsx:19). Design-system fact: Ant Design ships this detail-display pattern as `Descriptions`.
- Accept `ProgressMeter` -> `Progress`: locked by the topic, and #82 separately identifies `Progress` as a P1 missing primitive; the current component is already a 0-100 progress bar, so the rename is canonical rather than cosmetic (debate/ui7-scope-vs-issue82/00-Topic.md:8, debate/ui7-scope-vs-issue82/context/issue-82.md:18, src/components/data-display/progress-meter.tsx:3).
- Accept `CardStat` -> `StatCard`: locked by the topic, and the current implementation explicitly describes itself as a KPI/stat tile, so the new name describes the actual component instead of making it a `Card` sub-flavor (debate/ui7-scope-vs-issue82/00-Topic.md:8, src/components/data-display/card.tsx:169).

- Add `Avatar` in 7.0.0: #82 says a production migrator had to vendor standard primitives locally and lists Radix Avatar as P1; the repo currently has avatar upload behavior inside `Upload`, but no display-only `Avatar` export in the UI barrel (debate/ui7-scope-vs-issue82/context/issue-82.md:5, debate/ui7-scope-vs-issue82/context/issue-82.md:16, src/components/ui/index.tsx:1, src/components/ui/index.tsx:31, src/components/data-entry/upload.tsx:330). Design-system fact: Radix and shadcn expose `Avatar`/`AvatarImage`/`AvatarFallback`.
- Add root `Separator` in 7.0.0: #82 lists Radix Separator as P1; the repo only has menu/select separators scoped inside those components, not a public layout/content separator primitive (debate/ui7-scope-vs-issue82/context/issue-82.md:17, src/components/data-entry/select.tsx:159, src/components/navigation/dropdown-menu.tsx:91, src/components/ui/index.tsx:1). Design-system fact: Radix and shadcn expose `Separator`.
- Add generic `Skeleton` in 7.0.0: #82 says v6 has shaped skeletons but not the underlying block; the source confirms `SkeletonBlock` is private and only shaped skeletons are exported (debate/ui7-scope-vs-issue82/context/issue-82.md:19, src/components/feedback/skeleton.tsx:8, src/components/feedback/index.ts:31). Design-system fact: shadcn exposes a generic `Skeleton`.
- Add `Toggle` and `ToggleGroup` in 7.0.0: #82 lists them as P1 Radix primitives, and the public UI barrel has `switch` but no toggle exports; toggle is a different pressed-state control from a boolean form switch (debate/ui7-scope-vs-issue82/context/issue-82.md:20, src/components/ui/index.tsx:25). Design-system fact: Radix and shadcn expose both `Toggle` and `ToggleGroup`.
- Add `AspectRatio` in 7.0.0: #82 lists Radix Aspect Ratio as P1, and the public UI barrel has no equivalent media-ratio primitive; this is a low-risk static wrapper, not an architectural rewrite (debate/ui7-scope-vs-issue82/context/issue-82.md:21, src/components/ui/index.tsx:1). Design-system fact: Radix and shadcn expose `AspectRatio`.
- Treat `Progress` as satisfied by the locked `ProgressMeter` -> `Progress` change rather than as a second new component: both the topic cleanup and #82 point at the same canonical primitive name (debate/ui7-scope-vs-issue82/00-Topic.md:8, debate/ui7-scope-vs-issue82/context/issue-82.md:18).

- Keep `Sheet` and do not rename it to `Drawer`: #82 explicitly says `Drawer` means a bottom-sheet using vaul and is distinct from existing side `Sheet`; the current `Sheet` implementation is a Radix Dialog-derived side panel with `side` variants and a default right-side panel (debate/ui7-scope-vs-issue82/context/issue-82.md:29, src/components/feedback/sheet.tsx:2, src/components/feedback/sheet.tsx:39, src/components/feedback/sheet.tsx:52). Design-system fact: shadcn/ui uses `Sheet` for side panels and `Drawer` for vaul-based drawers.
- Defer or add bottom-sheet `Drawer` separately, but never by renaming `Sheet`: Option B allows new `Drawer` now or later, while preserving a clean concept boundary for migrators (debate/ui7-scope-vs-issue82/00-Topic.md:24, debate/ui7-scope-vs-issue82/00-Topic.md:27).

- Reject `PageContainer` -> `PageShell` for 7.0.0: the current component is indeed a page shell with title, breadcrumb, body, and footer, but the change is a medium audit suggestion, not a P1 migrator gap; renaming it would churn a mandatory page wrapper with uncertain benefit (debate/ui7-scope-vs-issue82/context/audit-findings.md:5, src/components/layout/page-container.tsx:18, src/components/layout/page-container.tsx:40, src/components/layout/page-container.tsx:76). Assumption: router dependency cleanup can be handled later without a public name change.
- Reject `ResponsiveGrid` -> `SimpleGrid` for 7.0.0: the component is only a 2/3/4-column responsive grid, and while `SimpleGrid` is a known Chakra name, this is cosmetic compared with #82 P1 gaps and would break consumers for a thin wrapper (debate/ui7-scope-vs-issue82/context/audit-findings.md:7, src/components/layout/responsive-grid.tsx:3, src/components/layout/responsive-grid.tsx:10). Design-system fact: Chakra uses `SimpleGrid`, but MUI uses `Grid`, so the name is not universally canonical.
- Reject `Topbar` -> `AppBar` for 7.0.0: the implementation is domain-shaped with product/project/search/notifications/tweaks props, so a mere rename would not make it a canonical AppBar; the audit's real complaint is API shape, not only name (debate/ui7-scope-vs-issue82/context/audit-findings.md:10, src/components/layout/topbar.tsx:20, src/components/layout/topbar.tsx:109, src/components/layout/topbar.tsx:117, src/components/layout/topbar.tsx:131). Design-system fact: MUI uses `AppBar`; Carbon/Fluent usually use `Header`, so this is not a single naming standard.
- Reject broad `AppShell` redesign for 7.0.0: `AppShell` is already a standard Mantine term, and the current component is a layout shell; the mixed slot API can be improved later without bundling it into the P1 primitive fill (debate/ui7-scope-vs-issue82/context/audit-findings.md:8, src/components/layout/app-shell.tsx:8, src/components/layout/app-shell.tsx:19). Design-system fact: Mantine ships `AppShell`.
- Reject broad `Sidebar` redesign for 7.0.0: the current data-driven sections API is real, but replacing it with compound components would be a high-churn architectural rewrite unrelated to the #82 P1 primitive gap (debate/ui7-scope-vs-issue82/context/audit-findings.md:9, src/components/layout/sidebar.tsx:169, src/components/layout/sidebar.tsx:210).
- Reject `FilterBar` -> `FilterToolbar` / Toolbar split for 7.0.0: the component does mix layout, clear behavior, and translated copy, but the audit itself frames this as a redesign; ship the P1 primitive gap first and do not add another breaking migration target without direct migrator demand (debate/ui7-scope-vs-issue82/context/audit-findings.md:29, src/components/navigation/filter-bar.tsx:10, src/components/navigation/filter-bar.tsx:21).
- Reject `Select` API split/redesign for 7.0.0: the current `Select` deliberately supports both compound Radix and data-driven APIs; changing that is a real behavior/API rewrite, not a clearly-correct rename (src/components/data-entry/select.tsx:18, src/components/data-entry/select.tsx:25, src/components/data-entry/select.tsx:191). Design-system fact: Radix/shadcn prefer compound Select; Ant Design supports data-driven `options`, so there is no single universal API shape.
- Reject `Upload`/`AvatarUpload` split for 7.0.0: the source shows upload currently owns dropzone, button, picture-card, picture, avatar, and avatar-crop variants; extracting avatar upload may be cleaner, but it is a broad redesign, whereas #82 asks for display-only `Avatar` as P1 (debate/ui7-scope-vs-issue82/context/audit-findings.md:34, debate/ui7-scope-vs-issue82/context/issue-82.md:16, src/components/data-entry/upload.tsx:107, src/components/data-entry/upload.tsx:330).
- Reject `FormField` -> `Field` for 7.0.0: the audit severity is low and calls the current component well-designed; the rename is not worth hard-breaking consumers in the same release as locked cleanup and P1 additions (debate/ui7-scope-vs-issue82/context/audit-findings.md:21).
- Reject `CheckboxGroup.onChange` -> `onValueChange` for 7.0.0 unless already touched by the locked cleanup: the inconsistency with `RadioGroup` is real, but not part of #82 P1 and not a naming cleanup; it can wait for a focused form-control pass (debate/ui7-scope-vs-issue82/context/audit-findings.md:24, src/components/data-entry/checkbox-group.tsx:29, src/components/data-entry/radio.tsx:51).
- Reject `Dialog`/`AlertDialog` split as 7.0.0 scope: #82 marks composable `AlertDialog` as optional/low priority because `DialogConfirm` covers the common destructive-confirm case, so it should not displace P1 primitive work (debate/ui7-scope-vs-issue82/context/issue-82.md:44, debate/ui7-scope-vs-issue82/context/issue-82.md:45).
- Reject P2/P3 additions from #82 for 7.0.0 except possibly separate `Drawer`: Accordion, ContextMenu, HoverCard, Menubar, NavigationMenu, Resizable, Carousel, InputOTP, Combobox, PasswordInput, TimeInput, Rating, TagInput, and media-query hooks are valid backlog, but Option B's coherent release story is locked cleanup plus P1 primitive parity (debate/ui7-scope-vs-issue82/context/issue-82.md:23, debate/ui7-scope-vs-issue82/context/issue-82.md:33, debate/ui7-scope-vs-issue82/context/issue-82.md:41).

- Assumption: "P1 primitives" in Option B means `Avatar`, `Separator`, generic `Skeleton`, `Toggle`, `ToggleGroup`, `AspectRatio`, and `Progress` via the locked rename, not every component listed in #82.
- Assumption: the release can implement these as thin Radix/shadcn-style wrappers with local tokens, consistent with rules for Radix interactive primitives and shadcn-style ownership (mcp/src/data/rules.ts:16, mcp/src/data/rules.ts:17).
- Assumption: adding missing exports is lower migration risk than renaming existing exports because additions do not break current consumers, while hard renames do (debate/ui7-scope-vs-issue82/00-Topic.md:32, debate/ui7-scope-vs-issue82/00-Topic.md:34).
